# AI/TLDR — SEO Status & Strategy

Single source of truth for SEO work on this repo. Update this file whenever
you ship, remove, or plan an SEO change — future-you should be able to read
it top-to-bottom and know exactly what the site looks like to a crawler.

**Last updated**: 2026-06-15
**Public domain**: https://ai-tldr.dev
**Canonical host**: `ai-tldr.dev` (Cloudflare Worker)

> **Companion doc:** AI-era search — GEO/AEO, i.e. getting cited *inside*
> ChatGPT / Perplexity / Google AI Overviews — lives in
> [GEO_AEO_PLAYBOOK.md](GEO_AEO_PLAYBOOK.md). **This file** is the classic
> Google SERP; **the playbook** is AI answers. When they overlap, the playbook
> governs the AI surfaces.

## 1. Crawlable pages

| Path                    | Generator                  | Target keyword(s)                          | Schema                          |
| ----------------------- | -------------------------- | ------------------------------------------ | ------------------------------- |
| `/`                     | `scripts/prerender.ts`     | _new AI releases_, _AI release tracker_    | `WebSite` + `Organization`      |
| `/influencers`          | `scripts/prerender.ts`     | _top AI influencers to follow_             | `CollectionPage`                |
| `/log`                  | `scripts/prerender.ts`     | _AI release changelog_, _AI changelog_     | `CollectionPage`                |
| `/releases/<id>` (×56+) | `scripts/prerender.ts`     | item-specific (release name + org + tags)  | `Article` + `BreadcrumbList`    |
| `/sitemap.xml`          | `scripts/prerender.ts`     | —                                          | —                               |
| `/robots.txt`           | `scripts/prerender.ts`     | —                                          | —                               |

Cloudflare serves static files first and falls back to `/index.html` for
anything else (see `public/_redirects`), so the SPA router handles unknown
paths without 404-ing.

## 2. Meta tag strategy

- **Titles**: primary keyword first, pipe separator, brand suffix. Hard
  cap ≤ 60 chars so Google doesn't truncate the brand.
- **Descriptions**: 120–158 chars, natural keyword usage, include a
  concrete value prop ("refreshed every 8 hours", "110 creators ranked",
  etc). Never keyword-stuffed.
- **OG/Twitter**: auto-generated by `renderMetaBlock` in
  [scripts/prerender.ts](scripts/prerender.ts) from the same `PageMeta`
  object as the regular `<title>` + `<meta description>`. One source of
  truth per page.
- **Canonical**: always absolute, always `https://`, never trailing slash
  except `/`. Env-overridable via `SITE_URL`.
- **OG image**: per-release items use `item.image.url` (the verified
  release hero); home / `/influencers` / `/log` fall back to
  `${SITE_URL}/og-image.png` (ships in `public/`).

### Current page meta (as of 2026-04-13)

**Home** — `New AI Releases Daily — Models, Tools & Papers | AI/TLDR` (56ch)
> Every new AI model, repo, tool, and paper worth knowing — refreshed
> every 8 hours and explained in plain English. Track what's shipping today.

**Influencers** — `Top AI Influencers to Follow — 110 Creators Ranked | AI/TLDR` (60ch)
> The AI creators, researchers and newsletter authors worth following —
> ranked by reach across YouTube, X/Twitter, GitHub, podcasts, blogs and Substack.

**Log** — `AI Release Changelog — What Shipped & When | AI/TLDR` (52ch)
> The full changelog of AI/TLDR sweeps — every AI model, repo, tool,
> paper and dataset added, with a one-line note on why each was picked.

**Release pages** — built by `buildReleaseTitle()` in prerender:
1. `${title} — ${org} | AI/TLDR` (preferred)
2. `${title} | AI/TLDR` (drop org if #1 > 60ch)
3. `${title}` alone (if title itself already ≥ 60ch)

## 3. Schema / JSON-LD strategy

Each page ships exactly ONE JSON-LD block matching its content type. The
dev-mode fallback block in [index.html](index.html) is stripped during
prerender and replaced with the page-specific block before `</head>`.

| Page           | JSON-LD                                                             |
| -------------- | ------------------------------------------------------------------- |
| `/`            | `@graph` of `WebSite` (`@id:/#website`) + `Organization` (`@id:/#org`, with logo `ImageObject`) |
| `/influencers` | `CollectionPage` with `isPartOf` → `/#website`, `publisher` → `/#org` |
| `/log`         | `CollectionPage` (same refs as influencers)                         |
| `/releases/<id>` | `Article` (headline, description, datePublished, dateModified, author, publisher ref, image ImageObject, mainEntityOfPage, articleSection, keywords) **+** `BreadcrumbList` (Home → release title) |

All release Article schemas carry `dateModified` from `feed.generatedAt`
so crawlers get a signal every time the 8h cron refreshes the feed.

## 4. H1 hierarchy

Google expects exactly one `<h1>` per page describing the page topic.
Until 2026-04-13 the brand `AI/TLDR` was the `<h1>` on every page, which
meant `/influencers` and `/log` were signaling "AI/TLDR" as their topic
instead of their actual content.

Fixed in App.tsx — the brand renders as `<h1>` **only on the feed home**,
where "AI/TLDR" IS the page topic. On `/influencers` the brand is a `<p>`
and `<h1 class="inf-title">Top AI Influencers to Follow</h1>` is the
visible page title. Same pattern on `/log` with
`<h1 class="log-title">AI Release Changelog</h1>`.

CSS uses `text-transform: uppercase` so the visual stays brutalist while
the source text is sentence-case (what Google actually indexes).

## 5. Sitemap

Regenerated on every `bun run build` by `scripts/prerender.ts`. Includes:

- `/` — priority 1.0, daily
- `/influencers` — priority 0.8, weekly
- `/log` — priority 0.7, daily
- `/releases/<id>` — priority 0.6, monthly, `lastmod = item.date`

Total URLs at last build: **59** (3 index pages + 56 release pages).

## 6. Robots

[dist/robots.txt](dist/robots.txt) allows all crawlers on everything and
points at `${SITE_URL}/sitemap.xml`. No `Disallow` entries.

`<meta name="robots">` in the HTML shell is
`index, follow, max-image-preview:large, max-snippet:-1` — lets Google
show the release hero image in rich results and use full-length snippets
for the explainer copy.

## 7. Done — shipped optimizations

- [x] **2026-06-17** — **AI Release Index** (`/stats`): an original-data page —
  stat cards + extractable one-sentence takeaways + ranked tables/bars (top
  labs, categories, weekly cadence, importance, most-starred OSS tools). Built
  from the verified feed + landscape via `scripts/build-stats.ts` → `stats.json`
  (count.json pattern — keeps landscape out of the main bundle). Dataset +
  BreadcrumbList JSON-LD, in `sitemap-main.xml`, linked from the homepage body +
  footer. New URL-driven `/stats` route in `App.tsx` (Back works). `StatsPage`
  rendered to static HTML by `prerender.ts` via `renderToStaticMarkup`.
- [x] **2026-06-16** — `SearchAction` wired (see §8): `?q=` seeds the feed
  search box on mount, so `/?q=term` lands on filtered results.
- [x] **2026-06-15** — GEO/AEO pass (see [GEO_AEO_PLAYBOOK.md](GEO_AEO_PLAYBOOK.md)):
  - `Organization` `sameAs` (entity-linking to the verified source repo) on home + the dev fallback
  - `author.url` on Article / SoftwareApplication / SoftwareSourceCode / TechArticle / Dataset JSON-LD, via a curated, conservative org→homepage map (`ORG_HOMEPAGES`); unknown orgs ship no URL
  - `article:author` OG tag + self-referential `hreflang="x-default"` on every page
  - Atom feed at `/feed.xml` (last 50 releases) + `<link rel="alternate" type="application/atom+xml">` discovery in the template
  - `/llms.txt` + `/llms-full.txt` (cheap hedge — no major engine consumes it as of mid-2026)
  - Question-shaped release headings ("What is it?", "How does it work?", "Why does it matter?", "Who is it for?")
  - **"Related releases"** internal-link nav on every release page (scored by org/category/tags) — kills the crawl dead-end where releases were reachable only from home or the sitemap
  - Preconnect to the image CDNs (`cdn-uploads.huggingface.co`, `opengraph.githubassets.com`) for LCP
- [x] **2026-04-13** — Full on-page SEO pass:
  - Rewrote home, influencers, log titles + descriptions (keyword-first, ≤60ch)
  - Smart title truncation for release pages (drop org before truncating release name)
  - WebSite + Organization `@graph` on home with `@id` refs, absolute URLs
  - `CollectionPage` schema on `/influencers` and `/log`
  - Expanded `Article` schema on releases (dateModified, keywords, url, articleSection, ImageObject with dimensions, publisher @id ref, isPartOf @id ref)
  - Added `BreadcrumbList` to every release page
  - H1 hierarchy fix: brand is `<h1>` only on feed home; `/influencers` and `/log` now have their own semantic `<h1>`
  - Stripped template JSON-LD in prerender so each page ships exactly one page-specific block
  - `max-image-preview:large` + `max-snippet:-1` on robots meta
- [x] **2026-04-13** — Domain swap `blackpc-me.workers.dev` → `ai-tldr.dev` across prerender default, CLAUDE.md, and all generated canonical/OG/sitemap URLs
- [x] **2026-04-12** — Static prerendering: per-release HTML with per-item meta + Article JSON-LD ([scripts/prerender.ts](scripts/prerender.ts))
- [x] **2026-04-12** — Sitemap.xml + robots.txt generation
- [x] **2026-04-12** — `public/_redirects` for Cloudflare SPA fallback
- [x] **2026-04-12** — Dynamic `document.title` updates on client-side route changes
- [x] **2026-04-12** — OG image fallback `public/og-image.png` (1200×630) ships in every build

## 8. Open / planned

Ordered by impact.

### High impact

- [x] **Add a real RSS/Atom feed at `/feed.xml`** — ✅ shipped 2026-06-15 (Atom 1.0, last 50 releases; see §7).
- [ ] **Submit sitemap to Google Search Console + Bing Webmaster Tools** — one-time. Set up the property on `ai-tldr.dev` and verify via DNS or HTML file. Bing also accepts the sitemap via IndexNow. _User action, not code._
- [ ] **Claim the brand on X/Twitter and add `<meta name="twitter:site" content="@ai_tldr">`** — once the handle exists. Improves Twitter card attribution. _Code change is 1 line in `renderMetaBlock`._
- [ ] **Per-release OG image validation**: during prerender, HEAD-check each `item.image.url` and warn if it doesn't return `image/*`. Prevents a bad agent run from shipping broken social cards. _~15 min of work._

### Medium impact

- [x] **`SearchAction` in WebSite JSON-LD** — ✅ shipped 2026-06-16. `App.tsx` now seeds the feed search box from `?q=` on mount (`parseQueryFromUrl`), so `/?q=term` lands on filtered results — the Google sitelinks search-box entry point. Kept minimal/URL-driven: search stays UI-local otherwise (no feed-scroll regression).
- [x] **Add `author.url` to release Article schema** — ✅ shipped 2026-06-15 via `ORG_HOMEPAGES` curated map (Article + Software + TechArticle + Dataset).
- [ ] **`DefinedTerm` / `ItemList` JSON-LD on `/influencers`** so each creator becomes a discoverable entity with `sameAs` links to their social profiles. Currently the page is just a generic `CollectionPage`.
- [ ] **`FAQPage` schema on selected tall release explainers** where the explainer has clearly Q&A-like content. Big CTR lift if it earns a rich result.
- [x] **Internal linking → related releases** — ✅ shipped 2026-06-15 as a static "Related releases" nav (scored by org/category/tags) in the prerendered release body, so crawlers follow it. A live React rail on the *modal* is still open (UI work).

### Lower impact / nice to have

- [x] **hreflang `x-default`** — ✅ shipped 2026-06-15 (self-referential, every page).
- [x] **Preconnect to `cdn-uploads.huggingface.co`, `opengraph.githubassets.com`** — ✅ shipped 2026-06-15 (+ dns-prefetch for `i.ytimg.com`, `pbs.twimg.com`).
- [ ] **Skip-link for accessibility** (also a ranking signal for accessibility tests).
- [ ] **`<link rel="preload" as="font">`** for JetBrains Mono + Inter — _deferred: Google Fonts woff2 URLs are hashed/unstable; preconnect to gstatic (already present) is the safe win._
- [x] **`article:author` OG tag** on release pages — ✅ shipped 2026-06-15 (set to `item.org`).
- [ ] **Break cache-busted URLs out of sitemap** — not currently an issue but watch when asset URLs get long.

## 9. Maintenance playbook

**Whenever a new PAGE TYPE ships** (new route, new content surface):

1. Add a new `PageMeta` constant in `scripts/prerender.ts`.
2. Add a matching JSON-LD helper (or reuse `renderJsonLdCollectionPage`).
3. Wire it into `main()` with `injectMeta(template, META, jsonLd)`.
4. Add the page to the `buildSitemap()` call.
5. Add the route to `parseRoute()` in `src/App.tsx` and update the
   `document.title` effect.
6. Add a semantic `<h1>` to the component, and make sure the brand is
   NOT an `<h1>` when that page is active.
7. Update sections 1–3 of this file.
8. `bun run build` and verify the new `dist/<path>/index.html` has the
   right `<title>`, meta description, canonical, JSON-LD type.

**Whenever the AGENT runs a sweep** (every 8h via cron):

- `releases.json` gets rewritten. No SEO action needed — prerender
  regenerates all release pages + sitemap on the next build.
- `sweeps.json` gets one new entry. `/log` regenerates automatically.

**Whenever the PUBLIC DOMAIN changes**:

- Update `SITE_URL` default in [scripts/prerender.ts](scripts/prerender.ts).
- Update absolute URLs hardcoded in [index.html](index.html) JSON-LD fallback.
- Update references in [CLAUDE.md](CLAUDE.md) and this file.
- Set up a 301 redirect from the old host (Cloudflare rule).
- Submit the new sitemap to Search Console, retire the old property.
- Grep for stale URLs: `grep -rn "old-host" . --include="*.{ts,tsx,json,md,html,yml}"`.

## 10. Verification commands

Quick sanity checks after any SEO change:

```bash
# Full build (tsc + vite + prerender)
bun run build

# Titles + descriptions on all index pages
grep -E "<title>|description" dist/index.html dist/influencers/index.html dist/log/index.html

# JSON-LD presence + type on each page type
grep '"@type"' dist/index.html
grep '"@type"' dist/influencers/index.html
grep '"@type"' dist/log/index.html
grep '"@type"' dist/releases/google-gemma-4/index.html

# One JSON-LD script per static page; two per release page (Article + Breadcrumb)
for f in dist/index.html dist/influencers/index.html dist/log/index.html dist/releases/google-gemma-4/index.html; do
  printf "%s: " "$f"; grep -c "application/ld+json" "$f"
done

# Sitemap sanity
grep -c "<url>" dist/sitemap.xml
head -20 dist/sitemap.xml

# No stale domain references
grep -rn "workers.dev\|blackpc-me" dist/ 2>/dev/null | head
```

**External validators** (use after deploy):

- [Google Rich Results Test](https://search.google.com/test/rich-results)
- [Schema Markup Validator](https://validator.schema.org/)
- [Twitter Card Validator](https://cards-dev.twitter.com/validator) (cached)
- [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)
- [Google PageSpeed / Lighthouse](https://pagespeed.web.dev/)
