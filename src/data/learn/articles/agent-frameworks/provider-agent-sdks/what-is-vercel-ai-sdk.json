{
  "slug": "what-is-vercel-ai-sdk",
  "title": "What Is the Vercel AI SDK?",
  "shortTitle": "Vercel AI SDK",
  "seoTitle": "What Is the Vercel AI SDK? TypeScript AI Toolkit",
  "metaDescription": "The Vercel AI SDK is a TypeScript toolkit for streaming, tool calling, and structured output across providers. Learn its core parts and where it fits.",
  "keywords": [
    "what is the vercel ai sdk",
    "vercel ai sdk tutorial",
    "vercel ai sdk streaming tool calling",
    "ai sdk generatetext streamtext",
    "typescript ai sdk for agents"
  ],
  "difficulty": "beginner",
  "oneLiner": "Understand what the Vercel AI SDK gives you — a provider-agnostic TypeScript layer for streaming, tool calls, and structured output — and when it's the right starting point.",
  "updated": "2026-06-13",
  "sections": [
    {
      "id": "in-plain-english",
      "title": "In plain English",
      "blocks": [
        {
          "type": "p",
          "md": "The **Vercel AI SDK** is a TypeScript toolkit for talking to AI models from your own code. It gives you a small set of functions — most importantly `generateText` and `streamText` — that send a prompt to a model and get the answer back, whether you want the whole reply at once or word-by-word as it's written. It runs on the server (Node.js, Next.js, Bun, Cloudflare Workers) and ships React hooks for building the chat UI in the browser."
        },
        {
          "type": "p",
          "md": "Think of it as a universal remote for AI models. Every model provider — OpenAI, Anthropic, Google, and dozens more — has its own SDK, its own request shape, and its own quirks. The Vercel AI SDK puts one consistent set of buttons in front of all of them. You write `generateText` once; swapping from one model to another is a one-line change to the `model` argument, not a rewrite."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "Two things share the \"AI SDK\" name. The **core** (`ai` package) is provider-agnostic and runs anywhere TypeScript runs. The **UI** part (`@ai-sdk/react` and friends) is the React layer that wires a streaming response into a chat component. You can use the core alone on a backend with no React at all."
        }
      ]
    },
    {
      "id": "why-it-matters",
      "title": "Why it matters",
      "blocks": [
        {
          "type": "p",
          "md": "If you're building an AI feature in a JavaScript or TypeScript app, you face the same boring plumbing every time: format the request for *this* provider, open a streaming connection, parse the server-sent events, accumulate the tokens, handle [tool calls](/learn/llm-apis/function-calling/what-is-function-calling), retry on errors, and pipe it all to the browser without the UI freezing. The Vercel AI SDK does that plumbing so you can focus on the feature."
        },
        {
          "type": "p",
          "md": "Three problems it solves stand out:"
        },
        {
          "type": "list",
          "items": [
            "**Provider lock-in.** Without an abstraction layer, your code is welded to one vendor's SDK. Switching providers — or letting users pick a model — means touching every call site. The AI SDK's *provider adapters* normalize all of them behind one interface, so the model becomes a swappable argument.",
            "**Streaming is hard to do by hand.** Token-by-token streaming is what makes a chatbot feel alive, but doing it manually means managing SSE parsing, backpressure, and partial JSON. `streamText` returns a ready-made stream, and the React hooks consume it with no glue code.",
            "**Structured output and tools are fiddly.** Getting a model to return clean, typed JSON — or to call your functions — involves schema wrangling and validation. `generateObject` and the SDK's tool support handle the schema, the validation, and the loop for you."
          ]
        },
        {
          "type": "p",
          "md": "Who cares? Anyone shipping a chat interface, an [AI agent](/learn/ai-agents/agent-fundamentals/what-is-an-ai-agent), a \"summarize this\" button, or an extraction pipeline in a TypeScript stack. It is, in practice, the default starting point for AI features on Next.js and the broader JS ecosystem — and higher-level frameworks like Mastra are built on top of it."
        }
      ]
    },
    {
      "id": "how-it-works",
      "title": "How it works",
      "blocks": [
        {
          "type": "p",
          "md": "The SDK has three layers stacked on top of each other. You usually only touch the top one, but knowing all three explains why model-swapping is so cheap."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "stack",
            "title": "The three layers of the Vercel AI SDK",
            "layers": [
              {
                "label": "Core functions",
                "sub": "generateText, streamText, generateObject — what you call",
                "accent": true
              },
              {
                "label": "Provider adapters",
                "sub": "@ai-sdk/openai, @ai-sdk/anthropic, … — one normalized interface"
              },
              {
                "label": "UI hooks",
                "sub": "useChat, useCompletion — stream into React (optional)"
              }
            ]
          }
        },
        {
          "type": "h3",
          "text": "The core functions"
        },
        {
          "type": "p",
          "md": "Everything starts with a model object from a provider adapter and a prompt. `generateText` waits for the full reply; `streamText` returns immediately with a stream you can iterate or pipe to the client. Both take the same arguments, so moving from one to the other is trivial."
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "generateText — the whole reply at once",
          "code": "import { generateText } from \"ai\";\nimport { anthropic } from \"@ai-sdk/anthropic\";\n\nconst { text } = await generateText({\n  model: anthropic(\"claude-opus-4-8\"),\n  prompt: \"Explain what a vector database is in two sentences.\",\n});\n\nconsole.log(text);"
        },
        {
          "type": "p",
          "md": "To switch providers, change the import and the `model` line — nothing else. The rest of your code doesn't know or care which model answered."
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "streamText — token by token",
          "code": "import { streamText } from \"ai\";\nimport { anthropic } from \"@ai-sdk/anthropic\";\n\nconst result = streamText({\n  model: anthropic(\"claude-opus-4-8\"),\n  prompt: \"Write a haiku about TypeScript.\",\n});\n\nfor await (const chunk of result.textStream) {\n  process.stdout.write(chunk); // prints each piece as it arrives\n}"
        },
        {
          "type": "h3",
          "text": "The request-to-render path"
        },
        {
          "type": "p",
          "md": "In a typical web app, the flow runs from a browser click, through a server route that calls the SDK, and back as a live stream the React hook renders. The diagram below traces one message."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "flow",
            "title": "One chat message, end to end",
            "steps": [
              {
                "label": "User types",
                "sub": "useChat in the browser"
              },
              {
                "label": "POST /api/chat",
                "sub": "your server route"
              },
              {
                "label": "streamText",
                "sub": "calls the model via an adapter"
              },
              {
                "label": "Stream back",
                "sub": "toUIMessageStreamResponse()"
              },
              {
                "label": "UI updates",
                "sub": "useChat renders tokens",
                "accent": true
              }
            ]
          }
        },
        {
          "type": "p",
          "md": "The server route is tiny: it reads the incoming messages, calls `streamText`, and returns the result as a streaming HTTP response. The `useChat` hook on the client manages the message list, the input box, the loading state, and the incremental rendering — so the component stays declarative."
        }
      ]
    },
    {
      "id": "worked-example-a-streaming-chat-route",
      "title": "A worked example: a streaming chat route",
      "blocks": [
        {
          "type": "p",
          "md": "Here is the smallest useful chat feature — a server route plus a client component. The server piece is just a few lines because the SDK formats the stream into a shape the React hook understands automatically."
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "app/api/chat/route.ts — the server",
          "code": "import { streamText, convertToModelMessages, type UIMessage } from \"ai\";\nimport { anthropic } from \"@ai-sdk/anthropic\";\n\nexport async function POST(req: Request) {\n  const { messages }: { messages: UIMessage[] } = await req.json();\n\n  const result = streamText({\n    model: anthropic(\"claude-opus-4-8\"),\n    messages: convertToModelMessages(messages),\n  });\n\n  // Formats the stream so the useChat hook can read it directly.\n  return result.toUIMessageStreamResponse();\n}"
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "the client component",
          "code": "\"use client\";\nimport { useChat } from \"@ai-sdk/react\";\nimport { useState } from \"react\";\n\nexport default function Chat() {\n  const { messages, sendMessage } = useChat();\n  const [input, setInput] = useState(\"\");\n\n  return (\n    <div>\n      {messages.map((m) => (\n        <p key={m.id}><b>{m.role}:</b> {m.parts.map((p) =>\n          p.type === \"text\" ? p.text : null)}</p>\n      ))}\n      <form onSubmit={(e) => { e.preventDefault(); sendMessage({ text: input }); setInput(\"\"); }}>\n        <input value={input} onChange={(e) => setInput(e.target.value)} />\n      </form>\n    </div>\n  );\n}"
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Notice there is no SSE parsing, no token accumulation, and no manual loading flag. `streamText` produces the stream, `toUIMessageStreamResponse()` formats it, and `useChat` consumes it. That division — server function plus matching client hook — is the SDK's core idea."
        }
      ]
    },
    {
      "id": "structured-output-and-the-agent-loop",
      "title": "Structured output, tools, and the agent loop",
      "blocks": [
        {
          "type": "p",
          "md": "Plain text is only half the job. Two more functions cover the cases that make the SDK useful for real applications."
        },
        {
          "type": "h3",
          "text": "Typed JSON with generateObject"
        },
        {
          "type": "p",
          "md": "When you need data, not prose, `generateObject` constrains the model to a schema (defined with Zod) and gives you back a fully typed object. No parsing, no \"please return valid JSON\" prompt-begging — see [structured outputs explained](/learn/llm-apis/streaming-structured-outputs/structured-outputs-explained) for the underlying idea."
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "generateObject — schema-validated output",
          "code": "import { generateObject } from \"ai\";\nimport { anthropic } from \"@ai-sdk/anthropic\";\nimport { z } from \"zod\";\n\nconst { object } = await generateObject({\n  model: anthropic(\"claude-opus-4-8\"),\n  schema: z.object({\n    title: z.string(),\n    tags: z.array(z.string()),\n    sentiment: z.enum([\"positive\", \"neutral\", \"negative\"]),\n  }),\n  prompt: \"Classify this review: 'Fast shipping, but the box was dented.'\",\n});\n\nconsole.log(object.sentiment); // typed as the enum — no casting"
        },
        {
          "type": "h3",
          "text": "Tools and the loop"
        },
        {
          "type": "p",
          "md": "To make a model *do* things — fetch data, call an API, run a calculation — you give it [tools](/learn/ai-agents/tool-use/what-is-tool-use). Each tool is a function with a Zod input schema and an `execute` callback. The SDK runs the loop for you: the model picks a tool, the SDK runs it, feeds the result back, and repeats until the model produces a final answer. Set `stopWhen` with `stepCountIs` to cap the number of steps. That loop — call a tool, observe the result, decide again — is exactly the [agent](/learn/ai-agents/agent-fundamentals/what-is-an-ai-agent) pattern."
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "a tool-using call",
          "code": "import { generateText, tool, stepCountIs } from \"ai\";\nimport { anthropic } from \"@ai-sdk/anthropic\";\nimport { z } from \"zod\";\n\nconst { text } = await generateText({\n  model: anthropic(\"claude-opus-4-8\"),\n  prompt: \"What is the weather in Paris right now?\",\n  tools: {\n    getWeather: tool({\n      description: \"Get the current weather for a city.\",\n      inputSchema: z.object({ city: z.string() }),\n      execute: async ({ city }) => `It is 18°C and clear in ${city}.`,\n    }),\n  },\n  stopWhen: stepCountIs(5), // safety cap on the agent loop\n});"
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "This built-in tool loop is why the SDK is a natural agent foundation. Frameworks like Mastra add workflows, memory, and orchestration *on top of* this layer — they assume the AI SDK underneath rather than replacing it."
        }
      ]
    },
    {
      "id": "vercel-ai-sdk-vs-the-alternatives",
      "title": "How it compares to the alternatives",
      "blocks": [
        {
          "type": "p",
          "md": "The AI SDK sits in a specific spot: a thin, TypeScript-native layer, lower-level than a full agent framework but higher-level than each provider's raw SDK."
        },
        {
          "type": "table",
          "headers": [
            "Approach",
            "Best for",
            "Trade-off"
          ],
          "rows": [
            [
              "Provider's own SDK",
              "Deep use of one vendor's unique features",
              "Code is locked to that provider; you build streaming and tools yourself"
            ],
            [
              "**Vercel AI SDK**",
              "Provider-agnostic chat, streaming, tools, structured output in JS/TS",
              "Thin layer — you still wire up storage, multi-step orchestration, and memory"
            ],
            [
              "Full agent framework (e.g. Mastra)",
              "Workflows, agent memory, and orchestration out of the box",
              "More opinionated and heavier; built on the AI SDK anyway"
            ],
            [
              "Python frameworks (LangChain, etc.)",
              "Python-centric data and ML pipelines",
              "Wrong language for a TypeScript/Next.js front end"
            ]
          ]
        },
        {
          "type": "p",
          "md": "The short version: reach for the AI SDK when you want one clean interface over many models inside a TypeScript app, and you're happy to assemble the higher-level pieces yourself. Reach for a framework on top of it when you want those pieces pre-built. Reach for the raw provider SDK only when you need a niche feature the abstraction doesn't expose yet."
        }
      ]
    },
    {
      "id": "going-deeper",
      "title": "Going deeper",
      "blocks": [
        {
          "type": "p",
          "md": "The four functions above cover most day-to-day work, but a few areas reward a closer look once the basics click."
        },
        {
          "type": "p",
          "md": "**The provider registry and model strings.** Beyond importing each adapter directly, you can register providers centrally and refer to models by string (like `\"anthropic:claude-opus-4-8\"`). This makes \"let the user choose a model\" a configuration concern instead of a code change — handy when you support many models or want to switch defaults without redeploying logic."
        },
        {
          "type": "p",
          "md": "**Streaming objects, not just text.** `streamObject` is the structured-output cousin of `streamText`: it streams a JSON object as the model builds it, so a UI can render fields the moment they're ready instead of waiting for the whole object. Useful for forms, dashboards, and any \"fill in the blanks as you go\" interface."
        },
        {
          "type": "p",
          "md": "**Edge and serverless runtimes.** Because the core is built on web standards (the Fetch API and `ReadableStream`), it runs on Cloudflare Workers, Vercel's Edge runtime, Deno, and Bun — not just Node.js. This is a real advantage over SDKs that assume a Node-only environment, and it's why streaming works the same way close to the user."
        },
        {
          "type": "p",
          "md": "**Telemetry, retries, and middleware.** Production features hide one level down: automatic retries on transient errors, an `abortSignal` to cancel a request, OpenTelemetry tracing for observability, and a *language-model middleware* layer for wrapping calls with logging, caching, or guardrails. You don't need these on day one, but they're why the SDK scales from a demo to a real service."
        },
        {
          "type": "p",
          "md": "**The honest limits.** The AI SDK is deliberately *not* an agent framework. It gives you a tool loop, but it does not give you durable workflows, long-term memory, or multi-agent orchestration — you build those, or adopt a framework that provides them. Provider adapters also expose a normalized common surface; a brand-new model capability sometimes lands in the raw provider SDK first and reaches the adapter a little later. Knowing where the abstraction ends tells you when to drop down a layer."
        }
      ]
    }
  ],
  "faq": [
    {
      "q": "What is the Vercel AI SDK used for?",
      "a": "It's a TypeScript toolkit for adding AI features to JavaScript and TypeScript apps. You use it to call language models, stream replies token by token, get typed JSON back, and build chat UIs in React — all through one consistent interface that works across many model providers."
    },
    {
      "q": "What is the difference between generateText and streamText?",
      "a": "`generateText` waits for the model to finish and returns the complete reply at once — good for short, non-interactive tasks. `streamText` returns immediately with a stream you can read piece by piece — good for chat UIs where you want text to appear as it's generated. They take the same arguments, so switching between them is a one-line change."
    },
    {
      "q": "Is the Vercel AI SDK only for Next.js?",
      "a": "No. The core `ai` package is framework-agnostic and runs on any TypeScript backend — Node.js, Bun, Deno, Cloudflare Workers, and serverless edge runtimes. The React UI hooks (`useChat`, `useCompletion`) are convenient with Next.js but optional; you can use the core with any frontend or none at all."
    },
    {
      "q": "Does the Vercel AI SDK work with Claude and OpenAI?",
      "a": "Yes. It uses provider adapters — `@ai-sdk/anthropic` for Claude, `@ai-sdk/openai` for OpenAI, plus adapters for Google, Mistral, and many others. You import the adapter and pass its model to a core function. Switching providers is usually a one-line change to the `model` argument."
    },
    {
      "q": "Can the Vercel AI SDK build AI agents?",
      "a": "It gives you the core agent loop: define tools with schemas, and the SDK calls a tool, feeds the result back to the model, and repeats until done (capped with `stopWhen`). For richer agents with durable workflows, memory, and orchestration, frameworks like Mastra build on top of the AI SDK rather than replacing it."
    },
    {
      "q": "How do I get structured JSON from the Vercel AI SDK?",
      "a": "Use `generateObject` (or `streamObject` to stream it). You pass a Zod schema describing the shape you want, and the SDK constrains the model's output to it and returns a fully typed object — no manual JSON parsing or validation needed."
    }
  ],
  "related": [
    "what-is-claude-agent-sdk",
    "what-is-openai-agents-sdk",
    "what-is-agent-framework",
    "what-is-langchain",
    "agent-framework-comparison",
    "choose-agent-framework"
  ],
  "furtherReading": [
    {
      "title": "Vercel AI SDK — Official Documentation",
      "url": "https://ai-sdk.dev/docs"
    },
    {
      "title": "AI SDK Core — generateText and streamText",
      "url": "https://ai-sdk.dev/docs/ai-sdk-core"
    },
    {
      "title": "vercel/ai — GitHub repository",
      "url": "https://github.com/vercel/ai"
    },
    {
      "title": "Anthropic Provider for the AI SDK",
      "url": "https://ai-sdk.dev/providers/ai-sdk-providers/anthropic"
    }
  ]
}
