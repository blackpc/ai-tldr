{
  "slug": "openai-agents-sdk-handoffs",
  "title": "OpenAI Agents SDK: Handoffs, Guardrails & Tracing",
  "shortTitle": "OpenAI Agents SDK Handoffs",
  "seoTitle": "OpenAI Agents SDK: Handoffs, Guardrails & Tracing",
  "metaDescription": "The OpenAI Agents SDK is a lightweight framework for multi-agent workflows, with agent handoffs, guardrails, and built-in tracing.",
  "keywords": [
    "openai agents sdk",
    "what is openai agents sdk",
    "openai agents sdk tutorial",
    "openai agents handoffs",
    "openai swarm successor",
    "multi-agent workflow openai",
    "openai agents guardrails"
  ],
  "difficulty": "intermediate",
  "oneLiner": "You will understand how the OpenAI Agents SDK coordinates multiple agents through handoffs, enforces guardrails, and traces runs, and how it relates to the earlier Swarm experiment.",
  "updated": "2026-06-14",
  "sections": [
    {
      "id": "in-plain-english",
      "title": "In plain English",
      "blocks": [
        {
          "type": "p",
          "md": "The **OpenAI Agents SDK** is a small, open-source framework (Python and TypeScript) for building apps where *several* AI agents work together instead of one big agent doing everything. It is OpenAI's production-ready successor to their earlier experimental *Swarm* project, and it boils the whole problem down to a handful of primitives: **agents**, **handoffs**, **guardrails**, and built-in **tracing**. This article zooms in on the three that make multi-agent systems actually reliable — handoffs, guardrails, and tracing."
        },
        {
          "type": "image",
          "url": "/learn-media/dlg-643.jpg",
          "alt": "OpenAI Agents SDK Handoffs — illustration",
          "caption": "OpenAI Agents SDK Handoffs",
          "credit": "firebasestorage.googleapis.com"
        },
        {
          "type": "p",
          "md": "Picture a hospital front desk. A patient walks in and describes a problem. The receptionist does not try to diagnose anything — they figure out *who* should handle it and send the patient to the right department: cardiology, dermatology, billing. That redirection is a **handoff**: control of the conversation passes to a specialist who takes over completely, with the patient's history already in hand."
        },
        {
          "type": "p",
          "md": "Now add the rules pinned to the wall — things no department is allowed to do, like prescribe a drug a patient is allergic to. Those are **guardrails**: automatic checks that run alongside the agents and pull the emergency brake if something crosses a line. And the patient's chart, where every visit, test, and referral is logged so anyone can reconstruct what happened, is **tracing**. The Agents SDK gives you all three as real objects you configure in code, plus a runtime called the **Runner** that drives the whole flow."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "If you have never seen the SDK before, start with [what the OpenAI Agents SDK is](/learn/agent-frameworks/provider-agent-sdks/what-is-openai-agents-sdk/) for the gentle overview. This page assumes you know the basics and goes deeper on the *mechanics* of handoffs, guardrails, and tracing."
        }
      ]
    },
    {
      "id": "why-it-matters",
      "title": "Why it matters",
      "blocks": [
        {
          "type": "p",
          "md": "One agent with one giant prompt and twenty tools sounds simple, but it falls apart in practice. The instructions get long and contradictory, the model picks the wrong tool, and a single context window cannot hold the knowledge for billing *and* refunds *and* technical support *and* legal. The natural fix is to split the work across focused agents — but the moment you do that, three hard problems appear at once."
        },
        {
          "type": "list",
          "items": [
            "**Routing.** Something has to decide *which* agent handles a given message, and pass the conversation along without losing context. Hand-rolling this with `if/else` logic gets brittle fast. Handoffs make routing a first-class, model-driven decision.",
            "**Safety.** Once agents can call tools and take actions, you need checks that block bad input before it reaches a model and bad output before it reaches a user. Guardrails are that safety layer, built into the runtime rather than scattered through your code.",
            "**Observability.** Multi-agent runs are hard to debug — a single user message can trigger several model calls, tool calls, and handoffs. When something goes wrong, you need a replay. Tracing records every step automatically so you can see exactly what happened."
          ]
        },
        {
          "type": "p",
          "md": "The value of the SDK is that it makes these three concerns *standard* instead of bespoke. Before frameworks like this, every team reinvented their own routing glue, their own ad-hoc safety checks, and their own logging. Getting them as named primitives means less code to write, fewer ways to get it subtly wrong, and a shared vocabulary other developers already understand."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "Multi-agent design is not always the answer. A single well-scoped agent is simpler to build and test. Reach for handoffs when one agent genuinely cannot cover the domains well — see [provider SDK vs full framework](/learn/agent-frameworks/provider-agent-sdks/provider-sdk-vs-framework/) for where this kind of lightweight SDK fits."
        }
      ]
    },
    {
      "id": "how-it-works",
      "title": "How it works",
      "blocks": [
        {
          "type": "p",
          "md": "Everything runs inside the **Runner**. You call `Runner.run(agent, input)` and the Runner takes over the loop: it sends the conversation to the model, executes any tool the model asks for, follows any handoff the model decides to make, runs the guardrails, and stops when the active agent produces a final answer. You get back a result object holding the full history of what happened."
        },
        {
          "type": "h3",
          "text": "A handoff is just a special tool call"
        },
        {
          "type": "p",
          "md": "The key insight: a handoff is *not* magic — it is a tool. When you list other agents in an agent's `handoffs` parameter, the SDK quietly exposes each one to the model as a tool named something like `transfer_to_billing_agent`. The model, reading the conversation, decides whether to call that tool. If it does, the Runner swaps the **active agent** to the target and replays the *entire conversation history* into it, so the new agent has full context and continues talking to the user directly. The first agent is now out of the loop."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "flow",
            "title": "Anatomy of one handoff",
            "steps": [
              {
                "label": "User message",
                "sub": "arrives at triage agent"
              },
              {
                "label": "Triage agent + model",
                "sub": "reads message, picks a route"
              },
              {
                "label": "Handoff tool call",
                "sub": "transfer_to_billing_agent"
              },
              {
                "label": "Runner switches active agent",
                "sub": "full history passed along"
              },
              {
                "label": "Billing agent answers",
                "sub": "responds to user directly",
                "accent": true
              }
            ]
          }
        },
        {
          "type": "p",
          "md": "Because the model makes the decision, routing adapts to messy real-world phrasing far better than keyword rules. The user does not have to say \"billing\" — they can say \"my card got charged twice\" and the triage agent infers the right specialist. This is the same loop whether you have two agents or twenty."
        },
        {
          "type": "h3",
          "text": "Guardrails wrap the run, not the model"
        },
        {
          "type": "p",
          "md": "A **guardrail** is a function that inspects either the input *to* the first agent or the output *from* the last agent, and returns an object with a `tripwire_triggered` boolean. When the tripwire is `True`, the Runner immediately raises an exception (`InputGuardrailTripwireTriggered` or `OutputGuardrailTripwireTriggered`) and stops the run. Your code catches it and returns a safe fallback instead of letting a bad message or bad answer through. Crucially, input guardrails can run *in parallel* with the agent so they add little latency."
        },
        {
          "type": "h3",
          "text": "Tracing records the whole thing"
        },
        {
          "type": "p",
          "md": "Every `Runner.run` automatically emits a **trace**: a structured record of each model call, tool call, handoff, and guardrail check, tied together by a single trace ID. By default these go to the OpenAI dashboard for a visual replay, but an open processor interface lets you ship traces to other observability tools instead. You do not instrument anything — it is on by default."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "stack",
            "title": "What the Runner manages on every call",
            "layers": [
              {
                "label": "Guardrails",
                "sub": "input + output tripwires"
              },
              {
                "label": "Handoffs",
                "sub": "switch the active agent"
              },
              {
                "label": "Agents + tools",
                "sub": "model calls, tool execution"
              },
              {
                "label": "Tracing",
                "sub": "records every step automatically",
                "accent": true
              }
            ]
          }
        }
      ]
    },
    {
      "id": "a-worked-handoff-example",
      "title": "A worked handoff example",
      "blocks": [
        {
          "type": "p",
          "md": "Here is a triage system in a few lines. A triage agent decides between two specialists; an input guardrail blocks off-topic messages before any specialist runs. Notice how little wiring it takes — you just *declare* the relationships."
        },
        {
          "type": "code",
          "lang": "bash",
          "title": "Install",
          "code": "pip install openai-agents   # Python 3.10+\nexport OPENAI_API_KEY=sk-..."
        },
        {
          "type": "code",
          "lang": "python",
          "title": "triage.py",
          "code": "import asyncio\nfrom agents import (\n    Agent, Runner, InputGuardrail,\n    GuardrailFunctionOutput, RunContextWrapper,\n)\nfrom pydantic import BaseModel\n\n\n# --- An input guardrail (itself a tiny agent) ---\nclass TopicCheck(BaseModel):\n    is_off_topic: bool\n\nguard_agent = Agent(\n    name=\"Topic Guard\",\n    instructions=\"Is this message NOT about billing or tech support?\",\n    output_type=TopicCheck,\n)\n\nasync def check_topic(ctx: RunContextWrapper, agent, user_input):\n    res = await Runner.run(guard_agent, user_input, context=ctx.context)\n    return GuardrailFunctionOutput(\n        output_info=res.final_output,\n        tripwire_triggered=res.final_output.is_off_topic,\n    )\n\n\n# --- Specialists the triage agent can hand off to ---\nbilling = Agent(name=\"Billing\", instructions=\"Answer billing questions.\")\ntech = Agent(name=\"Tech Support\", instructions=\"Troubleshoot step by step.\")\n\n# --- Triage agent: routes + runs the guardrail ---\ntriage = Agent(\n    name=\"Triage\",\n    instructions=\"Route billing issues to Billing, technical issues to Tech Support.\",\n    handoffs=[billing, tech],\n    input_guardrails=[InputGuardrail(guardrail_function=check_topic)],\n)\n\n\nasync def main():\n    result = await Runner.run(triage, \"My card was charged twice this month.\")\n    print(result.final_output)        # answered by the Billing agent\n    print(result.last_agent.name)     # -> \"Billing\"\n\nasyncio.run(main())"
        },
        {
          "type": "p",
          "md": "At runtime the triage agent recognises a billing problem, calls the hidden `transfer_to_Billing` tool, and the Runner switches the active agent. The billing agent answers the user directly with full context. If the user had typed something unrelated, the guardrail's tripwire would fire in parallel and you would catch the exception before the billing agent ever ran."
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Want to customise the transfer? Wrap the target in the `handoff()` helper to override the tool name, add a description the model reads when deciding, run an `on_handoff` callback (for logging or fetching data), or define an `input_type` so the handing-off agent must pass structured data (like a `reason`) to the receiver."
        }
      ]
    },
    {
      "id": "handoffs-vs-agents-as-tools",
      "title": "Handoffs vs agents-as-tools",
      "blocks": [
        {
          "type": "p",
          "md": "There are two ways for one agent to use another, and beginners constantly mix them up. They produce very different conversation shapes, so choosing the right one is a real design decision."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "compare",
            "title": "Two ways agents collaborate",
            "columns": [
              {
                "title": "Handoff",
                "accent": true,
                "items": [
                  "Transfers ownership of the conversation",
                  "Receiving agent replies to the user directly",
                  "Original agent steps out of the loop",
                  "Full history is passed along",
                  "Best for: routing to a specialist who should take over"
                ]
              },
              {
                "title": "Agent as a tool",
                "items": [
                  "Orchestrator stays in charge",
                  "Specialist returns a result, not a reply",
                  "Orchestrator folds the result into its own answer",
                  "Only the result comes back",
                  "Best for: gathering and synthesising multiple specialists"
                ]
              }
            ]
          }
        },
        {
          "type": "table",
          "headers": [
            "Question",
            "Use a handoff",
            "Use agent-as-tool"
          ],
          "rows": [
            [
              "Who talks to the user next?",
              "The new agent",
              "The same orchestrator"
            ],
            [
              "Do you need several results combined?",
              "No",
              "Yes"
            ],
            [
              "Should the first agent stay in control?",
              "No",
              "Yes"
            ],
            [
              "Typical shape",
              "Customer-support triage",
              "Research assistant calling sub-tasks"
            ]
          ]
        },
        {
          "type": "p",
          "md": "A rule of thumb: if the specialist should *become* the assistant the user is talking to, hand off. If you want a coordinator that asks a few experts and writes the final answer itself, expose those experts as tools instead. You can mix both in one system."
        }
      ]
    },
    {
      "id": "common-pitfalls",
      "title": "Common pitfalls",
      "blocks": [
        {
          "type": "list",
          "items": [
            "**Vague handoff descriptions.** The model routes based on each agent's name and handoff description. If two agents sound similar (\"Support\" and \"Help\"), the model picks wrong. Give each a clear, distinct description of *when* to route there.",
            "**Expecting guardrails on every hop.** Input guardrails only run on the *first* agent in a chain; output guardrails only run on the *last*. A check you assumed protected a middle agent may never fire. For per-step safety, attach a guardrail to a specific tool instead.",
            "**Loops between agents.** If agent A can hand off to B and B can hand back to A, a confused model can ping-pong. Set a sensible `max_turns` on the run so a runaway loop stops instead of burning tokens forever.",
            "**Forgetting context cost.** Each handoff replays the full conversation into the next agent. In long chains that history grows, raising token cost and latency. Keep instructions tight and prune history where you can.",
            "**Trusting routing blindly.** The model's routing is good, not perfect. Use tracing to review real runs and check the right specialist actually handled each case before you trust it in production."
          ]
        },
        {
          "type": "callout",
          "tone": "warn",
          "md": "The SDK runs in *your* process with *your* API key, and agents can call tools that touch real systems. Scope each agent's tools to the minimum it needs, and put an output guardrail in front of any action that writes data or spends money. A misrouted agent with broad permissions is a real risk, not a hypothetical one."
        }
      ]
    },
    {
      "id": "going-deeper",
      "title": "Going deeper",
      "blocks": [
        {
          "type": "p",
          "md": "Once the three primitives feel natural, a few advanced directions are worth knowing."
        },
        {
          "type": "h3",
          "text": "Structured handoff inputs"
        },
        {
          "type": "p",
          "md": "By default a handoff just transfers the conversation. But you can attach an `input_type` (a Pydantic model) so the handing-off agent is forced to fill in structured fields — for example a `reason` for an escalation or a `ticket_id`. The receiving agent then starts with clean, typed data instead of having to re-parse the conversation. This makes multi-agent flows much more predictable."
        },
        {
          "type": "h3",
          "text": "Filtering what gets passed on"
        },
        {
          "type": "p",
          "md": "Passing the entire history to every downstream agent is not always what you want — sometimes earlier tool noise just confuses the specialist. A handoff can take an `input_filter` that trims or reshapes the history before the new agent sees it (for example, dropping prior tool-call clutter). Less noise in means cleaner reasoning out."
        },
        {
          "type": "h3",
          "text": "Tool-level guardrails"
        },
        {
          "type": "p",
          "md": "Because input and output guardrails only cover the ends of a chain, the SDK also supports guardrails attached directly to a tool. These fire every time that specific function is called, no matter which agent invoked it — ideal for blocking a database write above a row limit or a payment over a threshold, deep inside a multi-agent flow."
        },
        {
          "type": "h3",
          "text": "Routing to other ecosystems"
        },
        {
          "type": "p",
          "md": "The same handoff/guardrail/tracing pattern shows up across provider SDKs, with different trade-offs. If you are comparing, see [provider agent SDK comparison](/learn/agent-frameworks/provider-agent-sdks/provider-agent-sdk-comparison/), the [Claude Agent SDK](/learn/agent-frameworks/provider-agent-sdks/what-is-claude-agent-sdk/), and [Strands Agents](/learn/agent-frameworks/provider-agent-sdks/what-is-strands-agents/). The OpenAI SDK also speaks the Model Context Protocol, so agents in a run can use tools served by any MCP-compatible server without you writing wrappers."
        },
        {
          "type": "p",
          "md": "The honest open challenges remain. Model-driven routing is only as good as the model's judgment, so evaluation and tracing are not optional extras — they are how you find the cases where a handoff went to the wrong place. And every handoff trades a cleaner architecture for more model calls and more passed-around context. The durable lesson: keep each agent narrow, name and describe your handoffs precisely, guard the actions that matter, and watch real traces before you trust the system in production."
        }
      ]
    }
  ],
  "faq": [
    {
      "q": "How do handoffs work in the OpenAI Agents SDK?",
      "a": "You list target agents in an agent's `handoffs` parameter, and the SDK exposes each as a hidden tool like `transfer_to_billing_agent`. When the model decides to route, it calls that tool; the Runner then switches the active agent and replays the full conversation history into it. The new agent answers the user directly and the original agent steps out of the loop."
    },
    {
      "q": "What is the difference between a handoff and using an agent as a tool?",
      "a": "A handoff transfers ownership of the conversation — the receiving agent replies to the user directly. Agent-as-tool keeps an orchestrator in charge: it calls a specialist like any other tool and folds the returned result into its own answer. Use handoffs when a specialist should take over; use agent-as-tool when one coordinator should gather and synthesise several results."
    },
    {
      "q": "Do guardrails run on every agent in a handoff chain?",
      "a": "No. Input guardrails run only on the first agent in the chain, and output guardrails run only on the last. Agents in the middle are not automatically covered. If you need a safety check on a specific step, attach a guardrail directly to the relevant tool instead, since tool guardrails fire every time that function is called."
    },
    {
      "q": "Is the OpenAI Agents SDK the same as OpenAI Swarm?",
      "a": "They share the same conceptual model but are different libraries. Swarm was explicitly labelled experimental and educational. The Agents SDK is the production-ready successor with a typed API, guardrail machinery, structured outputs, and built-in tracing, and it is actively maintained. Build on the Agents SDK rather than Swarm."
    },
    {
      "q": "How do I stop two agents from handing off to each other forever?",
      "a": "Set a `max_turns` limit on the run so the Runner stops after a fixed number of steps instead of looping indefinitely. It also helps to give each agent precise handoff descriptions so the model is less likely to bounce control back and forth, and to review traces for ping-pong patterns before going to production."
    },
    {
      "q": "Can I customise what happens during a handoff?",
      "a": "Yes. Wrap the target agent in the `handoff()` helper to override the tool name, add a description the model reads when deciding, run an `on_handoff` callback, require structured `input_type` data, or apply an `input_filter` that trims the history the receiving agent sees. These let you control routing precisely instead of relying on defaults."
    }
  ],
  "related": [
    "what-is-openai-agents-sdk",
    "what-is-claude-agent-sdk",
    "provider-agent-sdk-comparison",
    "provider-sdk-vs-framework",
    "what-is-strands-agents"
  ],
  "furtherReading": [
    {
      "title": "openai-agents-python — GitHub repository",
      "url": "https://github.com/openai/openai-agents-python"
    },
    {
      "title": "OpenAI Agents SDK — official documentation",
      "url": "https://openai.github.io/openai-agents-python/"
    },
    {
      "title": "Model Context Protocol — specification",
      "url": "https://modelcontextprotocol.io/"
    }
  ]
}
