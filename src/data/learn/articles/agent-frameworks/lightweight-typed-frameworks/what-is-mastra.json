{
  "slug": "what-is-mastra",
  "title": "What Is Mastra? TypeScript Agents Beyond the Vercel AI SDK",
  "seoTitle": "What Is Mastra? TypeScript Agent Framework Guide",
  "metaDescription": "Mastra is a TypeScript framework for agents with workflows, memory, and evals built in. See how it extends the Vercel AI SDK into a full agent stack.",
  "keywords": [
    "what is mastra",
    "mastra typescript agent framework",
    "mastra vs vercel ai sdk",
    "mastra workflows tutorial",
    "build ai agents in typescript"
  ],
  "difficulty": "intermediate",
  "oneLiner": "Learn what Mastra adds on top of the Vercel AI SDK — durable workflows, agent memory, and built-in evals — for full agent apps in TypeScript.",
  "updated": "2026-06-12",
  "sections": [
    {
      "id": "in-plain-english",
      "title": "In plain English",
      "blocks": [
        {
          "type": "p",
          "md": "The Vercel AI SDK is a great engine. It gives you a unified interface over OpenAI, Anthropic, Google, and dozens of other model providers, plus streaming helpers and structured output. But an engine alone does not make a car. You still need seats, a steering wheel, a fuel tank, and a map."
        },
        {
          "type": "image",
          "url": "/learn-media/dlg-183.jpg",
          "alt": "Mastra — diagram",
          "caption": "Mastra",
          "credit": "mastra.ai"
        },
        {
          "type": "p",
          "md": "**Mastra** is the assembled car. It is an open-source TypeScript framework built *on top of* the Vercel AI SDK that adds the primitives real production agents need: durable multi-step workflows, persistent memory across sessions, a built-in eval harness for catching regressions, RAG pipeline utilities, and a local dev studio for inspecting agent state. The team behind it previously built Gatsby, the React static-site framework."
        },
        {
          "type": "p",
          "md": "Think of Mastra the way you think of Next.js relative to React. React renders UI; Next.js adds routing, server-side rendering, API routes, and a deployment model. Mastra follows the same pattern: the Vercel AI SDK handles the LLM call; Mastra adds the application layer that turns a single chat completion into a dependable, observable agent system."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "Mastra launched out of Y Combinator (W25 batch), hit v1.0 in January 2026, and had raised $35 million total (a $13M seed in late 2025 and a $22M Series A in April 2026 led by Spark Capital). It surpassed 22,000 GitHub stars and 1.8 million monthly npm downloads by early 2026. Engineering teams at Replit, PayPal, and Plaid have adopted it."
        }
      ]
    },
    {
      "id": "why-it-matters",
      "title": "Why it matters",
      "blocks": [
        {
          "type": "p",
          "md": "The Vercel AI SDK solves the model-routing problem elegantly. But once you move past a chat UI demo and into a real product, you run into a wall of infrastructure problems it was never designed to solve."
        },
        {
          "type": "h3",
          "text": "The four gaps the AI SDK leaves open"
        },
        {
          "type": "list",
          "items": [
            "**Memory**: your agent forgets everything between sessions. Building a memory layer from scratch means picking a vector database, writing embedding logic, managing thread IDs, handling context-window overflow, and wiring all of it together.",
            "**Durability**: a multi-step agent that runs for several minutes will fail mid-execution the moment you redeploy your server. Nothing in the AI SDK preserves workflow state across restarts.",
            "**Evals**: you have no automated way to detect when a prompt change silently degrades output quality. Without evals, regressions reach users.",
            "**Observability**: tracing a multi-tool agent call through logs alone is painful. You need structured traces that link each LLM call, tool invocation, and step output into a single readable timeline."
          ]
        },
        {
          "type": "p",
          "md": "Mastra ships answers to all four gaps as first-class primitives rather than leaving you to assemble them from third-party libraries. The result is that a TypeScript developer can go from `npm create mastra@latest` to a production-grade agent with memory, workflows, and evals in hours instead of weeks."
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "If your only use case is a streaming chat UI backed by a single LLM call, the Vercel AI SDK alone is probably enough. Mastra pays off the moment you need durable multi-step execution, memory that outlives a session, or an automated quality gate before shipping a prompt change."
        }
      ]
    },
    {
      "id": "how-it-works",
      "title": "How it works",
      "blocks": [
        {
          "type": "p",
          "md": "Mastra organises every agent application around five primitives: **Agents**, **Tools**, **Workflows**, **Memory**, and **Evals**. You compose these in a central `Mastra` instance that wires them together and exposes a local dev studio."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "stack",
            "title": "The Mastra stack",
            "layers": [
              {
                "label": "Your Application",
                "sub": "Next.js, Express, Hono, SvelteKit, Astro"
              },
              {
                "label": "Mastra Framework",
                "sub": "Agents · Workflows · Memory · Evals · RAG",
                "accent": true
              },
              {
                "label": "Vercel AI SDK",
                "sub": "Model routing · Streaming · Structured output"
              },
              {
                "label": "LLM Providers",
                "sub": "OpenAI · Anthropic · Google · 90+ others"
              }
            ]
          }
        },
        {
          "type": "h3",
          "text": "Agents"
        },
        {
          "type": "p",
          "md": "A Mastra agent wraps an LLM with a system prompt, a set of tools, and optionally a memory instance. Agents use LLM reasoning to decide which tools to call and iterate until they reach a stopping condition. Because Mastra delegates to the Vercel AI SDK under the hood, every model supported by the AI SDK — OpenAI, Anthropic, Google, Mistral, xAI, and more — is automatically available."
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "Minimal Mastra agent",
          "code": "import { Mastra } from '@mastra/core';\nimport { Agent } from '@mastra/core/agent';\nimport { openai } from '@ai-sdk/openai';\n\nconst researchAgent = new Agent({\n  name: 'Research Agent',\n  instructions: 'You are a helpful research assistant.',\n  model: openai('gpt-4o'),\n});\n\nexport const mastra = new Mastra({\n  agents: { researchAgent },\n});"
        },
        {
          "type": "h3",
          "text": "Workflows"
        },
        {
          "type": "p",
          "md": "Where agents let the LLM decide execution order, Mastra **workflows** give you deterministic control. A workflow is a graph-based state machine built from typed steps. Each step declares its `inputSchema` and `outputSchema` using Zod (or Valibot, ArkType, or plain JSON Schema), so data flowing between steps is validated at every boundary."
        },
        {
          "type": "p",
          "md": "Steps chain together with `.then()` for sequential execution, `.branch()` for conditionals, and `.parallel()` for fan-out. The workflow is finalised with `.commit()` and started with `run.start()`. Workflows can also be **suspended** mid-execution — for a human approval step, an API webhook, or rate-limit throttling — and resumed later from the exact snapshot that was saved."
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "A two-step Mastra workflow",
          "code": "import { createStep, createWorkflow } from '@mastra/core/workflows';\nimport { z } from 'zod';\n\nconst fetchStep = createStep({\n  id: 'fetch',\n  inputSchema: z.object({ url: z.string() }),\n  outputSchema: z.object({ body: z.string() }),\n  execute: async ({ inputData }) => {\n    const res = await fetch(inputData.url);\n    return { body: await res.text() };\n  },\n});\n\nconst summariseStep = createStep({\n  id: 'summarise',\n  inputSchema: z.object({ body: z.string() }),\n  outputSchema: z.object({ summary: z.string() }),\n  execute: async ({ inputData, mastra }) => {\n    const agent = mastra.getAgent('researchAgent');\n    const result = await agent.generate(`Summarise: ${inputData.body}`);\n    return { summary: result.text };\n  },\n});\n\nexport const fetchAndSummarise = createWorkflow({\n  id: 'fetch-and-summarise',\n  inputSchema: z.object({ url: z.string() }),\n  outputSchema: z.object({ summary: z.string() }),\n})\n  .then(fetchStep)\n  .then(summariseStep)\n  .commit();"
        },
        {
          "type": "h3",
          "text": "Memory"
        },
        {
          "type": "p",
          "md": "Mastra ships `@mastra/memory` with three memory modes. **Working memory** stores structured facts about a user — name, preferences, goals — that persist across all conversations. **Semantic recall** embeds past messages and retrieves the most relevant ones by meaning when the context window would overflow. **Observational memory** uses a background compression agent to replace raw message history with a dense observation log, keeping the active context small while preserving long-term recall."
        },
        {
          "type": "p",
          "md": "Memory requires a storage backend. The quickstart uses `@mastra/libsql` (SQLite-compatible). Production deployments can swap in Postgres, Upstash Redis, or any community-maintained adapter. The storage layer also persists workflow snapshots, so suspend/resume works across application restarts and redeployments."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "flow",
            "title": "Agent with memory — request flow",
            "steps": [
              {
                "label": "Incoming message",
                "sub": "user + thread IDs"
              },
              {
                "label": "Memory retrieval",
                "sub": "semantic recall + working memory"
              },
              {
                "label": "Context assembly",
                "sub": "compress if over limit"
              },
              {
                "label": "LLM call",
                "sub": "via Vercel AI SDK",
                "accent": true
              },
              {
                "label": "Response + storage",
                "sub": "persist to backend"
              }
            ]
          }
        },
        {
          "type": "h3",
          "text": "Evals"
        },
        {
          "type": "p",
          "md": "Mastra treats evals as first-class primitives rather than a testing afterthought. You attach eval functions directly to an agent definition. Built-in scorers cover accuracy, answer relevance, context recall, and toxicity. Custom evals can use model-graded scoring, rule-based checks, or statistical measures. Evals run in CI and surface regressions before a prompt change ships to users."
        }
      ]
    },
    {
      "id": "mastra-vs-vercel-ai-sdk",
      "title": "Mastra vs the Vercel AI SDK",
      "blocks": [
        {
          "type": "p",
          "md": "The most common question about Mastra is whether it *replaces* the Vercel AI SDK or competes with it. The answer is neither: Mastra depends on the AI SDK and re-exports its streaming interface for use in React UIs. The `@mastra/ai-sdk` package provides drop-in route handlers so the same Mastra agent streams to `useChat` in a Next.js app without any glue code."
        },
        {
          "type": "table",
          "headers": [
            "Capability",
            "Vercel AI SDK alone",
            "Mastra"
          ],
          "rows": [
            [
              "Model routing (OpenAI, Anthropic, etc.)",
              "Yes",
              "Yes (delegates to AI SDK)"
            ],
            [
              "Streaming to React useChat / useCompletion",
              "Yes",
              "Yes (via @mastra/ai-sdk)"
            ],
            [
              "Structured output + tool calling",
              "Yes",
              "Yes"
            ],
            [
              "Durable multi-step workflows",
              "No",
              "Yes — createWorkflow"
            ],
            [
              "Persist memory across sessions",
              "No",
              "Yes — @mastra/memory"
            ],
            [
              "Built-in evals",
              "No",
              "Yes"
            ],
            [
              "RAG pipeline helpers",
              "No",
              "Yes"
            ],
            [
              "MCP server authoring",
              "No",
              "Yes"
            ],
            [
              "Local dev studio (Mastra Studio)",
              "No",
              "Yes"
            ],
            [
              "Suspend / resume mid-workflow",
              "No",
              "Yes"
            ]
          ]
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "Mastra also supports exporting agents as **MCP servers** via `@mastra/mcp`, so any host that understands the Model Context Protocol — Claude Desktop, Cursor, custom orchestrators — can invoke your Mastra agent's tools directly."
        }
      ]
    },
    {
      "id": "getting-started",
      "title": "Getting started in five minutes",
      "blocks": [
        {
          "type": "p",
          "md": "The fastest path is the Mastra CLI scaffold. It creates a project, installs dependencies, and drops in example agents and tools you can edit immediately."
        },
        {
          "type": "code",
          "lang": "bash",
          "title": "Scaffold a new Mastra project",
          "code": "# Scaffold with OpenAI, agents + tools components, and an example\nnpm create mastra@latest my-agent -- --components agents,tools --llm openai --example\n\ncd my-agent\nnpm install\n\n# Start the local dev studio at http://localhost:4111\nnpm run dev"
        },
        {
          "type": "p",
          "md": "Mastra Studio opens in the browser and shows every registered agent, lets you send test messages, inspect memory threads, trace tool calls step by step, and run evals interactively. It replaces the `console.log`-driven debug loop that most developers default to when building agents."
        },
        {
          "type": "h3",
          "text": "Adding memory to an existing agent"
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "Agent with persistent memory",
          "code": "import { Memory } from '@mastra/memory';\nimport { LibSQLStore } from '@mastra/libsql';\nimport { Agent } from '@mastra/core/agent';\nimport { openai } from '@ai-sdk/openai';\n\nconst memory = new Memory({\n  storage: new LibSQLStore({ url: 'file:local.db' }),\n  options: {\n    lastMessages: 20,\n    semanticRecall: { topK: 5 },\n  },\n});\n\nconst assistantAgent = new Agent({\n  name: 'Assistant',\n  instructions: 'You are a helpful assistant that remembers the user.',\n  model: openai('gpt-4o-mini'),\n  memory,\n});\n\n// Pass resource + thread IDs to scope memory per user/conversation\nconst response = await assistantAgent.generate(\n  'What did we talk about last week?',\n  { resourceId: 'user-123', threadId: 'thread-456' },\n);"
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "For local development, `LibSQLStore` writes to a SQLite file with zero config. Swap it for `@mastra/pg` (Postgres) before deploying to production — only the `storage` constructor line changes."
        }
      ]
    },
    {
      "id": "common-pitfalls",
      "title": "Common pitfalls and tradeoffs",
      "blocks": [
        {
          "type": "p",
          "md": "Mastra is a fast-moving framework that reached v1.0 in January 2026, so some rough edges remain. Knowing the common traps ahead of time saves debugging time."
        },
        {
          "type": "h3",
          "text": "Pitfalls to watch out for"
        },
        {
          "type": "list",
          "items": [
            "**Schema drift between taxonomy and articles.** If you use Mastra to build a content pipeline, keep your Zod schemas in sync across steps. Mastra validates at step boundaries, so a mismatch surfaces as a runtime error rather than a silent data corruption.",
            "**Workflow suspend/resume requires a persistent store.** Suspend only works if you configure a storage backend. Omitting storage causes suspended workflows to be lost on restart. The default in-memory store is development-only.",
            "**Agent memory and workflow state share the same storage.** For high-volume workloads, size your Postgres or LibSQL instance to handle both. Memory compression (observational memory) is recommended once threads grow past a few hundred messages.",
            "**Evals run sequentially by default.** For large eval suites, configure parallelism or use the Mastra cloud eval runner to avoid CI timeout issues.",
            "**MCP server exports are stateless.** When you expose a Mastra agent as an MCP server, memory is not automatically passed from the calling host. You must configure a shared storage backend and pass consistent resource/thread IDs from the client."
          ]
        },
        {
          "type": "h3",
          "text": "When Mastra is overkill"
        },
        {
          "type": "p",
          "md": "Mastra adds framework overhead — package installs, a `Mastra` instance, a storage backend, and a local studio process. For a simple one-off script that calls an LLM once and prints the result, the Vercel AI SDK alone is lighter and faster to set up. Mastra's value compounds as the agent grows: more steps, more sessions, more models, and more need for quality assurance."
        }
      ]
    },
    {
      "id": "going-deeper",
      "title": "Going deeper",
      "blocks": [
        {
          "type": "p",
          "md": "Once you are comfortable with the core primitives, Mastra has several advanced capabilities worth exploring."
        },
        {
          "type": "h3",
          "text": "Deploying to durable execution engines"
        },
        {
          "type": "p",
          "md": "By default, Mastra runs workflows in its built-in in-process engine. For workflows that must survive server restarts, deployment events, or long waits (hours, days), Mastra supports swapping in **Inngest** as a durable workflow runner. You configure an Inngest adapter on the workflow and Mastra handles the serialisation, scheduling, and state recovery. Your step code does not change."
        },
        {
          "type": "h3",
          "text": "Multi-agent networks"
        },
        {
          "type": "p",
          "md": "Mastra supports agent networks where a router agent delegates subtasks to specialised sub-agents. Each sub-agent can have its own model, tools, and memory scope. The router receives all sub-agent outputs and synthesises a final answer. This pattern scales to complex tasks — competitive research, code review pipelines, multi-language customer support — without requiring a monolithic agent prompt."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "split",
            "title": "Multi-agent network",
            "root": {
              "label": "Router Agent",
              "sub": "decides which agent handles the task",
              "accent": true
            },
            "children": [
              {
                "label": "Research Agent",
                "sub": "web search + summarisation"
              },
              {
                "label": "Code Agent",
                "sub": "code generation + testing"
              },
              {
                "label": "Data Agent",
                "sub": "SQL queries + analysis"
              }
            ]
          }
        },
        {
          "type": "h3",
          "text": "RAG with Mastra"
        },
        {
          "type": "p",
          "md": "Mastra ships RAG primitives for chunking documents, embedding them into a vector store, and querying by semantic similarity. Supported vector backends include Postgres with `pgvector`, Pinecone, Qdrant, and others via community adapters. The embedding step accepts any AI SDK-compatible embedding model, so you can use OpenAI `text-embedding-3-small`, Cohere, or a local model."
        },
        {
          "type": "h3",
          "text": "Observability and tracing"
        },
        {
          "type": "p",
          "md": "Every Mastra operation emits OpenTelemetry traces. Connect a compatible backend — Langfuse, Braintrust, Jaeger, or your existing OTEL collector — and you get end-to-end visibility: which model was called, which tools fired, how long each step took, and what the input/output was at every stage. For teams already on an OTEL stack, zero additional instrumentation code is required."
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Mastra Studio (the local dev UI) includes a built-in trace viewer, so you get observability during development without setting up an external backend. Point an OTEL exporter at a production collector when you deploy."
        },
        {
          "type": "h3",
          "text": "The model router"
        },
        {
          "type": "p",
          "md": "Mastra maintains a model index at [mastra.ai](https://mastra.ai/) listing over 3,300 models from 94 providers as of March 2026. Every model is accessible through the same Vercel AI SDK `LanguageModel` interface, so switching from `gpt-4o` to `claude-opus-4` or `gemini-2.0-flash` is a one-line change with no other code modification."
        }
      ]
    }
  ],
  "faq": [
    {
      "q": "Does Mastra replace the Vercel AI SDK?",
      "a": "No — Mastra is built *on top of* the Vercel AI SDK and depends on it. It uses the AI SDK for all LLM calls, model routing, and streaming. The `@mastra/ai-sdk` package re-exports streaming helpers so the same agent that runs in a Mastra workflow can also stream to a Next.js `useChat` hook."
    },
    {
      "q": "Is Mastra free and open source?",
      "a": "The core framework (`@mastra/core`, `@mastra/memory`, workflow engine, evals) is open source under the MIT/Apache-2.0 licence and free to self-host. Mastra also offers a managed cloud platform with hosted workflow execution and an eval dashboard, which is a separate commercial product."
    },
    {
      "q": "What storage backends does Mastra memory support?",
      "a": "Mastra ships official adapters for LibSQL (SQLite-compatible, great for local dev), Postgres, and Upstash Redis. The storage interface is pluggable, so community adapters exist for other databases. All three built-in options support both agent memory and workflow snapshot persistence."
    },
    {
      "q": "Can I use Mastra with models other than OpenAI?",
      "a": "Yes. Because Mastra delegates to the Vercel AI SDK, any model provider the AI SDK supports — Anthropic, Google, Mistral, Cohere, xAI, Groq, Amazon Bedrock, and local models via Ollama — works out of the box. You simply pass a different model object to the `model` field in your agent or step."
    },
    {
      "q": "How do Mastra workflows compare to plain async TypeScript functions?",
      "a": "Plain async functions have no persistence — if the process restarts mid-execution, you lose all state. Mastra workflows serialise each step's output to storage, so they survive deploys and crashes. They also give you type-validated data at step boundaries, structured traces, and suspend/resume for human-in-the-loop flows, none of which you get for free with raw async/await."
    },
    {
      "q": "What is Mastra Studio?",
      "a": "Mastra Studio is a local web UI (runs on `localhost:4111` by default) that starts alongside your dev server. It lets you chat with registered agents, inspect memory threads, step through workflow runs visually, view OTEL traces, and run evals interactively. It requires no external service — everything runs in-process during development."
    }
  ],
  "related": [
    "what-is-agent-framework",
    "choose-agent-framework",
    "agent-without-framework",
    "agent-framework-mental-models",
    "what-is-claude-agent-sdk"
  ],
  "furtherReading": [
    {
      "title": "Mastra official documentation",
      "url": "https://mastra.ai/docs"
    },
    {
      "title": "Using Mastra with the Vercel AI SDK",
      "url": "https://mastra.ai/blog/using-ai-sdk-with-mastra"
    },
    {
      "title": "Mastra on GitHub (mastra-ai/mastra)",
      "url": "https://github.com/mastra-ai/mastra"
    }
  ]
}
