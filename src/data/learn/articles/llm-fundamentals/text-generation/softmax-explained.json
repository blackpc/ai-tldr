{
  "slug": "softmax-explained",
  "title": "Softmax Function Machine Learning Guide: Turning Scores Into Probabilities",
  "seoTitle": "Softmax Function Machine Learning: Scores to Probabilities",
  "metaDescription": "Softmax function machine learning explained: it turns raw scores (logits) into probabilities that sum to 1. Learn the formula, temperature, and stability.",
  "keywords": [
    "softmax function machine learning",
    "what is softmax",
    "softmax formula explained",
    "softmax vs argmax",
    "softmax vs sigmoid",
    "softmax temperature",
    "softmax numerical stability"
  ],
  "difficulty": "intermediate",
  "oneLiner": "How softmax exponentiates and normalizes raw scores into a probability distribution that sums to 1.",
  "updated": "2026-06-12",
  "sections": [
    {
      "id": "in-plain-english",
      "title": "In plain English",
      "blocks": [
        {
          "type": "p",
          "md": "The **softmax function** is a small piece of math that takes a list of raw scores and turns them into a list of *probabilities* that add up to 1. Think of a panel of judges shouting numbers like `2.0`, `1.0`, and `0.1` for three contestants. Those numbers are not probabilities, they are just opinions on an arbitrary scale. Softmax is the scorekeeper who converts that messy chorus into a clean answer: contestant A wins **66%** of the vote, B gets **24%**, C gets **10%**."
        },
        {
          "type": "image",
          "url": "/learn-media/learn-010.jpg",
          "alt": "Typewriter Table",
          "caption": "Typewriter Table",
          "credit": "Kristin Hardwick"
        },
        {
          "type": "p",
          "md": "It does this in two moves: **exponentiate** every score (so bigger scores pull ahead and nothing stays negative), then **normalize** by dividing each one by the total. The result is always positive and always sums to 1, which is exactly what a probability distribution must do."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "In machine learning the raw input scores are usually called *logits*. Softmax is the standard bridge from logits to probabilities. See [what logits are](/learn/llm-fundamentals/text-generation/logits-explained) for the input side of this story."
        }
      ]
    },
    {
      "id": "why-it-matters",
      "title": "Why it matters",
      "blocks": [
        {
          "type": "p",
          "md": "Most classifiers and language models do their thinking in a unitless number space. A neural network's final layer might say *\"cat = 4.2, dog = 1.1, bird = -0.5\"*, but you cannot act on raw numbers like that. You cannot threshold them, compare them across examples, or feed them into a loss function that expects probabilities. Softmax is the universal adapter that makes those scores usable."
        },
        {
          "type": "list",
          "items": [
            "**Interpretability.** `0.91` means the model is 91% confident, a far more useful statement than a raw score of `4.2`.",
            "**Training.** The cross-entropy loss used to train classifiers needs a probability distribution as input. Softmax provides it.",
            "**Sampling.** In an [LLM](/learn/llm-fundamentals/llm-basics/what-is-an-llm), the next token is drawn from the softmax probabilities, which is how the model picks its next word.",
            "**Comparability.** Because the outputs always sum to 1, you can compare confidence across different inputs and models on the same scale."
          ]
        },
        {
          "type": "p",
          "md": "If you have ever wondered how a model goes from internal numbers to *\"the next word is probably 'the' with 31% likelihood\"*, the answer is almost always softmax. It is one of the most-used functions in all of deep learning."
        }
      ]
    },
    {
      "id": "how-it-works",
      "title": "How it works",
      "blocks": [
        {
          "type": "h3",
          "text": "The softmax function machine learning formula"
        },
        {
          "type": "p",
          "md": "Given a vector of scores `z = [z_1, z_2, ..., z_K]`, softmax computes the output for position `i` as `exp(z_i)` divided by the sum of `exp(z_j)` over every position `j`. In words: **raise e to the power of each score, then divide each result by the total of all of them.** The denominator is the *normalization term* that guarantees the outputs sum to exactly 1."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "flow",
            "title": "From raw scores to probabilities",
            "steps": [
              {
                "label": "Logits",
                "sub": "[2.0, 1.0, 0.1]"
              },
              {
                "label": "Exponentiate",
                "sub": "e^z: [7.39, 2.72, 1.11]",
                "accent": true
              },
              {
                "label": "Sum them",
                "sub": "7.39 + 2.72 + 1.11 = 11.22"
              },
              {
                "label": "Divide each by sum",
                "sub": "normalize"
              },
              {
                "label": "Probabilities",
                "sub": "[0.659, 0.242, 0.099] -> sum = 1"
              }
            ]
          }
        },
        {
          "type": "p",
          "md": "Two properties fall straight out of this recipe. First, every output lands strictly between 0 and 1, because `exp` is always positive and we divide by a sum that includes the numerator. Second, the outputs sum to 1 by construction. Together they make a valid probability distribution. A third, subtler property is that softmax **amplifies the largest score**: the gap between `2.0` and `1.0` (a difference of 1.0 in raw space) becomes a gap between `0.659` and `0.242` after exponentiation, because the exponential stretches differences apart."
        },
        {
          "type": "table",
          "headers": [
            "Logit z_i",
            "exp(z_i)",
            "Probability"
          ],
          "rows": [
            [
              "2.0",
              "7.39",
              "0.659"
            ],
            [
              "1.0",
              "2.72",
              "0.242"
            ],
            [
              "0.1",
              "1.11",
              "0.099"
            ]
          ]
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Softmax is *translation invariant*: add the same constant to every logit and the output is unchanged, because the constant cancels in the division. This harmless-looking fact is the key to the numerical-stability trick later in this article."
        }
      ]
    },
    {
      "id": "tiny-code-example",
      "title": "Softmax in a few lines of code",
      "blocks": [
        {
          "type": "p",
          "md": "Here is softmax written from scratch, plus the one-line library version most people actually use. Notice the `- np.max(z)` in the from-scratch version, which we explain in the next section."
        },
        {
          "type": "code",
          "lang": "python",
          "title": "softmax.py",
          "code": "import numpy as np\n\ndef softmax(z):\n    z = z - np.max(z)          # numerical stability (more below)\n    exp_z = np.exp(z)          # exponentiate every score\n    return exp_z / exp_z.sum() # normalize so outputs sum to 1\n\nlogits = np.array([2.0, 1.0, 0.1])\nprobs = softmax(logits)\nprint(probs)          # [0.659  0.242  0.099]\nprint(probs.sum())    # 1.0\n\n# In practice you'd reach for a library:\n# from scipy.special import softmax\n# from torch import softmax  # torch.softmax(t, dim=-1)"
        },
        {
          "type": "list",
          "ordered": true,
          "items": [
            "Subtract the max for stability (this does not change the result).",
            "Exponentiate each element.",
            "Divide by the sum so the vector becomes a probability distribution."
          ]
        }
      ]
    },
    {
      "id": "softmax-vs-argmax-vs-sigmoid",
      "title": "Softmax vs argmax vs sigmoid",
      "blocks": [
        {
          "type": "p",
          "md": "Softmax is easy to confuse with two neighbours. **Argmax** also points at the biggest score, but it returns a single hard winner with no notion of confidence. It is non-differentiable, so you cannot train through it with gradient descent, which is why models use the *soft* version during training and often fall back to argmax only at inference when they just need one answer (the so-called *greedy* choice, covered in [greedy decoding vs sampling](/learn/llm-fundamentals/text-generation/greedy-decoding-vs-sampling))."
        },
        {
          "type": "p",
          "md": "**Sigmoid** squashes one number into the range (0, 1) independently. It is the right tool when classes are not mutually exclusive (an image can be both *outdoor* and *sunny*). Softmax is the right tool when exactly one class should win and the probabilities must compete and sum to 1. A useful mental note: softmax over two classes is mathematically equivalent to a single sigmoid."
        },
        {
          "type": "table",
          "headers": [
            "Function",
            "Output",
            "Sums to 1?",
            "Differentiable?",
            "Use when"
          ],
          "rows": [
            [
              "softmax",
              "vector of probabilities",
              "Yes",
              "Yes",
              "one class wins among many"
            ],
            [
              "argmax",
              "index of the winner",
              "n/a",
              "No",
              "you only need the final pick"
            ],
            [
              "sigmoid",
              "one probability per class",
              "No",
              "Yes",
              "classes are independent (multi-label)"
            ]
          ]
        }
      ]
    },
    {
      "id": "softmax-inside-llms",
      "title": "Where softmax lives inside an LLM",
      "blocks": [
        {
          "type": "p",
          "md": "Large language models use softmax in **two completely different places**, which trips up a lot of newcomers."
        },
        {
          "type": "h3",
          "text": "1. The output layer (logits to next-token probabilities)"
        },
        {
          "type": "p",
          "md": "At the very end of the network, the model produces one logit per token in its vocabulary, often tens of thousands of them. Softmax turns that giant logit vector into a probability distribution over the whole vocabulary. The model then samples its [next token](/learn/llm-fundamentals/text-generation/next-token-prediction) from that distribution. This is the step that makes [how LLMs work](/learn/llm-fundamentals/llm-basics/how-llms-work) feel probabilistic rather than deterministic."
        },
        {
          "type": "h3",
          "text": "2. Inside attention"
        },
        {
          "type": "p",
          "md": "Deep inside every transformer layer, the attention mechanism scores how much each token should attend to every other token. Those scores are passed through softmax to become *attention weights* that sum to 1, so each token spends a fixed budget of attention across the sequence. The original *Attention Is All You Need* formula is `softmax(QK^T / sqrt(d_k)) V`, where the division by `sqrt(d_k)` keeps the scores from getting so large that softmax becomes overly peaked. See [how attention works](/learn/llm-fundamentals/transformers-and-attention/how-attention-works) for the full picture."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "split",
            "title": "Two softmax jobs in a transformer",
            "root": {
              "label": "Softmax in an LLM",
              "accent": true
            },
            "children": [
              {
                "label": "Attention weights",
                "sub": "inside every layer: who attends to whom"
              },
              {
                "label": "Output distribution",
                "sub": "final layer: probability of each next token"
              }
            ]
          }
        }
      ]
    },
    {
      "id": "going-deeper",
      "title": "Going deeper",
      "blocks": [
        {
          "type": "h3",
          "text": "Temperature: making softmax sharper or softer"
        },
        {
          "type": "p",
          "md": "You can divide every logit by a *temperature* `T` before applying softmax: `softmax(z / T)`. A **low** temperature (T < 1) makes the distribution sharper and more confident, pushing mass onto the top choice. A **high** temperature (T > 1) flattens it toward uniform, giving rarer options a chance. At `T = 1` you get plain softmax; as `T` approaches 0 softmax approaches argmax. This single knob is exactly the [temperature setting](/learn/llm-fundamentals/sampling-and-hallucination/llm-temperature-explained) you adjust in an LLM API to trade off focused versus creative output."
        },
        {
          "type": "table",
          "headers": [
            "Temperature",
            "Effect on distribution",
            "Behaviour"
          ],
          "rows": [
            [
              "T < 1 (e.g. 0.5)",
              "sharper, peakier",
              "more deterministic, repetitive"
            ],
            [
              "T = 1",
              "standard softmax",
              "model's natural distribution"
            ],
            [
              "T > 1 (e.g. 1.5)",
              "flatter, smoother",
              "more diverse, more random"
            ]
          ]
        },
        {
          "type": "h3",
          "text": "The numerical-stability max-subtraction trick"
        },
        {
          "type": "p",
          "md": "Computers store floating-point numbers with a finite range. Exponentiating a large logit like `1000` produces a number too big to represent, which becomes `inf` and poisons the whole computation (and very negative logits can underflow to `0`). The fix relies on the translation-invariance property from earlier: subtract the maximum logit from every element first. Now the largest exponent is `exp(0) = 1` and everything else is between 0 and 1, so nothing can overflow, *and the final probabilities are mathematically identical* to the naive version."
        },
        {
          "type": "code",
          "lang": "python",
          "title": "stable_vs_naive.py",
          "code": "import numpy as np\n\nz = np.array([1000.0, 1001.0, 1002.0])\n\n# Naive: overflows to inf, returns nan\nnaive = np.exp(z) / np.exp(z).sum()\nprint(naive)            # [nan nan nan]\n\n# Stable: subtract the max first, identical math, no overflow\nz_shift = z - z.max()   # [-2., -1., 0.]\nstable = np.exp(z_shift) / np.exp(z_shift).sum()\nprint(stable)           # [0.090  0.245  0.665]"
        },
        {
          "type": "callout",
          "tone": "warn",
          "md": "Never implement softmax for production without the max-subtraction step. Every mature framework does it internally, which is one more reason to use a library's built-in softmax rather than rolling your own in a hot training loop."
        },
        {
          "type": "h3",
          "text": "Cost at scale and what comes after"
        },
        {
          "type": "p",
          "md": "Over a vocabulary of 100,000+ tokens, computing a full softmax for every position of every training step is expensive, which has motivated approximations like hierarchical softmax and sampled softmax in older systems. Modern frameworks fuse the softmax with the cross-entropy loss (the *log-softmax* path) for both speed and stability. The takeaway: softmax is conceptually a two-line function, but squeezing it onto a [GPU](/learn/llm-fundamentals/llm-basics/why-llms-need-gpus) at frontier-model scale is a genuine engineering problem."
        }
      ]
    }
  ],
  "faq": [
    {
      "q": "What is the softmax function machine learning models use?",
      "a": "Softmax is a function that converts a vector of raw scores (logits) into a probability distribution. It exponentiates each score and divides by the total, so every output is positive and all outputs sum to 1. It is the standard bridge from a model's internal scores to interpretable probabilities."
    },
    {
      "q": "What is the difference between softmax and argmax?",
      "a": "Argmax returns only the index of the single largest score with no confidence value and is not differentiable, so you cannot train through it. Softmax returns a full probability distribution over all options, is differentiable, and preserves the ranking. Models train with softmax and may use argmax at inference for a single hard pick."
    },
    {
      "q": "When should I use softmax instead of sigmoid?",
      "a": "Use softmax when exactly one class should win and the probabilities must compete and sum to 1 (single-label classification, next-token prediction). Use sigmoid when classes are independent and more than one can be true at once (multi-label tasks). Softmax over two classes equals a single sigmoid."
    },
    {
      "q": "Why do you subtract the max in softmax?",
      "a": "Exponentiating a large logit can overflow to infinity and break the computation. Because softmax is unchanged when you add or subtract a constant from every input, subtracting the maximum logit makes the largest exponent exp(0) = 1 and prevents overflow, while producing mathematically identical probabilities."
    },
    {
      "q": "How does temperature change softmax?",
      "a": "Temperature T divides the logits before softmax. A temperature below 1 sharpens the distribution and makes the model more confident and deterministic; a temperature above 1 flattens it and increases diversity. At T = 1 you get standard softmax, and as T approaches 0 softmax approaches argmax."
    }
  ],
  "related": [
    "logits-explained",
    "llm-temperature-explained",
    "how-attention-works",
    "next-token-prediction",
    "greedy-decoding-vs-sampling"
  ],
  "furtherReading": [
    {
      "title": "Softmax function (Wikipedia)",
      "url": "https://en.wikipedia.org/wiki/Softmax_function"
    },
    {
      "title": "Attention Is All You Need (Vaswani et al., arXiv)",
      "url": "https://arxiv.org/abs/1706.03762"
    },
    {
      "title": "Softmax Regression (Dive into Deep Learning)",
      "url": "https://d2l.ai/chapter_linear-classification/softmax-regression.html"
    }
  ]
}
