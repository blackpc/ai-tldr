{
  "slug": "how-to-call-llm-api",
  "title": "How to Make Your First LLM API Call",
  "shortTitle": "Your First API Call",
  "seoTitle": "How to Make Your First LLM API Call: Step-by-Step",
  "metaDescription": "Step-by-step: install the SDK, authenticate with your API key, send a request, read the response, tune parameters, and handle errors in Python and JavaScript.",
  "keywords": [
    "how to call llm api",
    "openai api tutorial for beginners",
    "how to use anthropic api python",
    "llm api request tutorial",
    "first llm api call python javascript",
    "llm api authentication tutorial"
  ],
  "difficulty": "beginner",
  "oneLiner": "Walk through every step of a real LLM API call — authentication, request structure, response parsing, parameter tuning, multi-turn chat, and error handling — with working Python and JavaScript code.",
  "updated": "2026-06-12",
  "sections": [
    {
      "id": "in-plain-english",
      "title": "In plain English",
      "blocks": [
        {
          "type": "p",
          "md": "Making an LLM API call is a lot like ordering a taxi through an app. You open the app (install the SDK), enter your payment details once (your API key), type your destination (your message), and tap 'request ride' (send the call). A few seconds later a car arrives (the model's reply). You don't know which server ran the model or how the tokens were generated — none of that matters. You just needed to fill in the right form."
        },
        {
          "type": "image",
          "url": "/learn-media/dlg-057.jpg",
          "alt": "Make Your First LLM API Call — diagram",
          "caption": "Make Your First LLM API Call",
          "credit": "medium.com"
        },
        {
          "type": "p",
          "md": "This article is a hands-on walkthrough of that form. By the end you will have working code in **Python** and **JavaScript** that authenticates, sends a prompt, reads every useful field in the response, adjusts the key parameters, holds a multi-turn conversation, and recovers gracefully from errors. If you want to understand *what* an LLM API is before you start typing code, read [What Is an LLM API?](/learn/llm-apis/api-basics/what-is-llm-api) first."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "The examples below use **OpenAI** (Python `openai` package, `gpt-4o-mini`) and **Anthropic** (Python `anthropic` package, `claude-haiku-*`). Both providers share the same conceptual shape. Where the code differs between them, the difference is called out explicitly."
        }
      ]
    },
    {
      "id": "why-it-matters",
      "title": "Why it matters",
      "blocks": [
        {
          "type": "p",
          "md": "Reading a tutorial is one thing; shipping a working call is another. Most beginners get stuck at one of four predictable spots: setting up the API key without leaking it, understanding which field in the response actually holds the text, knowing which parameters to touch (and which to ignore), and recovering when the network or the provider misbehaves. This tutorial addresses all four in sequence."
        },
        {
          "type": "p",
          "md": "Getting past those four hurdles unlocks almost everything else in AI engineering. [RAG](/learn/rag/rag-fundamentals/what-is-rag) is retrieval + an API call. [Agents](/learn/ai-agents/agent-fundamentals/what-is-an-ai-agent) are loops of API calls plus tool use. Evals are batches of API calls scored against criteria. The pattern you learn here scales from a single prototype to a high-volume production system — only the error handling and retry logic get more sophisticated."
        }
      ]
    },
    {
      "id": "how-it-works",
      "title": "How each call travels",
      "blocks": [
        {
          "type": "p",
          "md": "Before touching code, here is the full path of one API call so you know exactly what every line of your code corresponds to."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "flow",
            "title": "Anatomy of one LLM API call",
            "steps": [
              {
                "label": "Your environment",
                "sub": "API key in env var, SDK installed"
              },
              {
                "label": "SDK call",
                "sub": "client.chat.completions.create(...)"
              },
              {
                "label": "HTTPS POST",
                "sub": "JSON body + Authorization header",
                "accent": true
              },
              {
                "label": "Provider gateway",
                "sub": "validates key, applies rate limits"
              },
              {
                "label": "Model inference",
                "sub": "tokens generated on GPU cluster"
              },
              {
                "label": "JSON response",
                "sub": "message text + usage counts"
              },
              {
                "label": "Your code",
                "sub": "reads .choices[0].message.content"
              }
            ]
          }
        },
        {
          "type": "p",
          "md": "Notice that three things happen before the model ever sees your text: the SDK serializes your Python/JS objects into a JSON body, it attaches your API key as an `Authorization` header, and it fires an HTTPS POST to the provider's endpoint. The response comes back as JSON too — the SDK deserializes it into objects you can dot-access. You interact only with the top and bottom of this chain."
        }
      ]
    },
    {
      "id": "setup",
      "title": "Step 1 — Set up your environment",
      "blocks": [
        {
          "type": "p",
          "md": "You need two things before writing a single line of app code: an API key and the right package installed."
        },
        {
          "type": "h3",
          "text": "Get an API key"
        },
        {
          "type": "list",
          "ordered": true,
          "items": [
            "**OpenAI** — sign in at [platform.openai.com](https://platform.openai.com), go to *API keys*, click *Create new secret key*.",
            "**Anthropic** — sign in at [console.anthropic.com](https://console.anthropic.com), go to *API Keys*, click *Create Key*.",
            "Copy the key immediately — most dashboards show it only once."
          ]
        },
        {
          "type": "callout",
          "tone": "warn",
          "md": "Never paste the key into your code or commit it to git. A leaked key means someone else charges requests to your account. Store it in an environment variable or a `.env` file that is listed in `.gitignore`."
        },
        {
          "type": "h3",
          "text": "Store the key as an environment variable"
        },
        {
          "type": "code",
          "lang": "bash",
          "title": "terminal",
          "code": "# macOS / Linux — add to ~/.zshrc or ~/.bashrc for persistence\nexport OPENAI_API_KEY=\"sk-...\"\nexport ANTHROPIC_API_KEY=\"sk-ant-...\"\n\n# Windows (PowerShell)\n$env:OPENAI_API_KEY = \"sk-...\"\n$env:ANTHROPIC_API_KEY = \"sk-ant-...\""
        },
        {
          "type": "h3",
          "text": "Install the SDK"
        },
        {
          "type": "code",
          "lang": "bash",
          "title": "install",
          "code": "# Python\npip install openai           # OpenAI SDK\npip install anthropic         # Anthropic SDK\n\n# JavaScript / TypeScript (npm)\nnpm install openai\nnpm install @anthropic-ai/sdk"
        }
      ]
    },
    {
      "id": "first-call",
      "title": "Step 2 — Send your first call and read the response",
      "blocks": [
        {
          "type": "p",
          "md": "Below is the minimal working call for each provider, with every field labelled. Run this once to confirm your key is wired up correctly."
        },
        {
          "type": "h3",
          "text": "Python — OpenAI"
        },
        {
          "type": "code",
          "lang": "python",
          "title": "openai_first_call.py",
          "code": "import os\nfrom openai import OpenAI\n\nclient = OpenAI(api_key=os.environ[\"OPENAI_API_KEY\"])  # reads env var\n\nresponse = client.chat.completions.create(\n    model=\"gpt-4o-mini\",          # which model to use\n    max_tokens=256,               # hard cap on reply length\n    messages=[\n        {\"role\": \"system\", \"content\": \"You are a helpful assistant.\"},\n        {\"role\": \"user\",   \"content\": \"What is the capital of France?\"}\n    ]\n)\n\n# --- Reading the response ---\nreply_text  = response.choices[0].message.content   # the model's answer\nfinish      = response.choices[0].finish_reason     # 'stop' | 'length' | 'content_filter'\nprompt_tok  = response.usage.prompt_tokens          # tokens you sent\noutput_tok  = response.usage.completion_tokens      # tokens the model generated\ntotal_tok   = response.usage.total_tokens           # prompt + completion\n\nprint(reply_text)\nprint(f\"Tokens used: {prompt_tok} in / {output_tok} out\")"
        },
        {
          "type": "h3",
          "text": "Python — Anthropic (Claude)"
        },
        {
          "type": "code",
          "lang": "python",
          "title": "anthropic_first_call.py",
          "code": "import os\nfrom anthropic import Anthropic\n\nclient = Anthropic(api_key=os.environ[\"ANTHROPIC_API_KEY\"])\n\nresponse = client.messages.create(\n    model=\"claude-haiku-4-5\",     # check docs for current model IDs\n    max_tokens=256,\n    system=\"You are a helpful assistant.\",  # system is a top-level field in Anthropic\n    messages=[\n        {\"role\": \"user\", \"content\": \"What is the capital of France?\"}\n    ]\n)\n\n# --- Reading the response ---\nreply_text  = response.content[0].text              # Anthropic uses .content list, not .choices\nstop_reason = response.stop_reason                  # 'end_turn' | 'max_tokens' | 'stop_sequence'\ninput_tok   = response.usage.input_tokens\noutput_tok  = response.usage.output_tokens\n\nprint(reply_text)\nprint(f\"Tokens used: {input_tok} in / {output_tok} out\")"
        },
        {
          "type": "h3",
          "text": "JavaScript — OpenAI"
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "openai_first_call.mjs",
          "code": "import OpenAI from \"openai\";\n\nconst client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });\n\nconst response = await client.chat.completions.create({\n  model: \"gpt-4o-mini\",\n  max_tokens: 256,\n  messages: [\n    { role: \"system\",  content: \"You are a helpful assistant.\" },\n    { role: \"user\",    content: \"What is the capital of France?\" }\n  ]\n});\n\nconst replyText = response.choices[0].message.content;\nconst { prompt_tokens, completion_tokens } = response.usage;\n\nconsole.log(replyText);\nconsole.log(`Tokens: ${prompt_tokens} in / ${completion_tokens} out`);"
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "If `finish_reason` (OpenAI) or `stop_reason` (Anthropic) comes back as `\"length\"` / `\"max_tokens\"`, the reply was cut off — raise `max_tokens` or split the task into smaller requests."
        },
        {
          "type": "h3",
          "text": "Key differences between providers"
        },
        {
          "type": "table",
          "headers": [
            "Field",
            "OpenAI",
            "Anthropic"
          ],
          "rows": [
            [
              "System prompt",
              "Message with `role: \"system\"` in the array",
              "Separate `system` top-level field"
            ],
            [
              "Reply text path",
              "`response.choices[0].message.content`",
              "`response.content[0].text`"
            ],
            [
              "Stop reason field",
              "`finish_reason` on `choices[0]`",
              "`stop_reason` on the response root"
            ],
            [
              "Token counts",
              "`usage.prompt_tokens` / `completion_tokens`",
              "`usage.input_tokens` / `output_tokens`"
            ]
          ]
        }
      ]
    },
    {
      "id": "parameters",
      "title": "Step 3 — Tune the key parameters",
      "blocks": [
        {
          "type": "p",
          "md": "Most parameters default to sensible values. These are the ones worth knowing on day one."
        },
        {
          "type": "h3",
          "text": "model"
        },
        {
          "type": "p",
          "md": "The string ID of the model you want. Smaller models (`gpt-4o-mini`, `claude-haiku-*`) are faster and cheaper; larger ones (`gpt-4o`, `claude-sonnet-*`) are smarter. Start small. Upgrading is a one-line change. Model IDs change as providers release new versions — always copy the current ID from the provider's docs page, not from a tutorial (including this one)."
        },
        {
          "type": "h3",
          "text": "max_tokens"
        },
        {
          "type": "p",
          "md": "The hard ceiling on how many tokens the model can generate. Does *not* make the model produce a long answer — it just stops it from going over. If you set it too low, replies get truncated mid-sentence. Output tokens are billed separately from input tokens and are typically more expensive, so this knob also controls cost."
        },
        {
          "type": "h3",
          "text": "temperature"
        },
        {
          "type": "p",
          "md": "Controls randomness on a 0–2 scale (OpenAI) or 0–1 scale (Anthropic). `0` makes the model deterministic — same input always gives the same output, ideal for factual lookups, data extraction, and classification. `1` is the typical creative-writing default. Values above 1 (OpenAI only) produce very diverse, sometimes incoherent output. For most apps, stay between 0 and 1. See [temperature explained](/learn/llm-fundamentals/sampling-and-hallucination/llm-temperature-explained)."
        },
        {
          "type": "h3",
          "text": "stop sequences"
        },
        {
          "type": "p",
          "md": "A list of strings that, when the model generates them, immediately halts output. Useful for parsing: if you ask for a numbered list and add `stop=[\"6.\"]`, the model won't generate item six even if `max_tokens` hasn't been reached. This gives you fine-grained control over reply shape without parsing tricks."
        },
        {
          "type": "code",
          "lang": "python",
          "title": "parameter_examples.py",
          "code": "# Factual, deterministic call — temperature 0\nfact_response = client.chat.completions.create(\n    model=\"gpt-4o-mini\",\n    max_tokens=100,\n    temperature=0,          # reproducible answers\n    messages=[{\"role\": \"user\", \"content\": \"What year was Python created?\"}]\n)\n\n# Creative call — temperature 0.9\nstory_response = client.chat.completions.create(\n    model=\"gpt-4o-mini\",\n    max_tokens=400,\n    temperature=0.9,        # varied, imaginative output\n    messages=[{\"role\": \"user\", \"content\": \"Write the opening line of a sci-fi novel.\"}]\n)\n\n# Stop after the first numbered item\nlist_response = client.chat.completions.create(\n    model=\"gpt-4o-mini\",\n    max_tokens=200,\n    stop=[\"2.\"],            # halts before item 2\n    messages=[{\"role\": \"user\", \"content\": \"List the planets starting with 1.\"}]\n)"
        }
      ]
    },
    {
      "id": "multi-turn",
      "title": "Step 4 — Build a multi-turn conversation",
      "blocks": [
        {
          "type": "p",
          "md": "The API is stateless: every call starts from scratch and the model has no memory of previous calls. To create a conversation, you maintain a list of messages in your code and extend it after each turn. The diagram below shows the pattern."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "cycle",
            "title": "Multi-turn conversation loop",
            "steps": [
              {
                "label": "User sends message",
                "sub": "append {role: user, content: ...}"
              },
              {
                "label": "POST full history",
                "sub": "all prior messages included",
                "accent": true
              },
              {
                "label": "Model replies",
                "sub": "reads the entire conversation"
              },
              {
                "label": "Append reply",
                "sub": "append {role: assistant, content: ...}"
              },
              {
                "label": "Wait for next input",
                "sub": "history grows each turn"
              }
            ]
          }
        },
        {
          "type": "code",
          "lang": "python",
          "title": "multi_turn.py",
          "code": "import os\nfrom openai import OpenAI\n\nclient = OpenAI(api_key=os.environ[\"OPENAI_API_KEY\"])\n\n# Start with the system instruction\nhistory = [\n    {\"role\": \"system\", \"content\": \"You are a concise Python tutor.\"}\n]\n\nwhile True:\n    user_input = input(\"You: \")\n    if user_input.lower() in (\"quit\", \"exit\"):\n        break\n\n    # 1. Append the new user message\n    history.append({\"role\": \"user\", \"content\": user_input})\n\n    # 2. Send the ENTIRE history\n    response = client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        max_tokens=512,\n        messages=history\n    )\n\n    reply = response.choices[0].message.content\n    print(f\"Assistant: {reply}\\n\")\n\n    # 3. Append the model's reply so future turns remember it\n    history.append({\"role\": \"assistant\", \"content\": reply})"
        },
        {
          "type": "callout",
          "tone": "warn",
          "md": "History grows with every turn and the whole thing is sent (and billed) each call. Long conversations eventually exceed the model's [context window](/learn/llm-fundamentals/context-windows/what-is-a-context-window). Common mitigations: trim the oldest messages, summarize earlier turns, or start a fresh session after N turns."
        }
      ]
    },
    {
      "id": "error-handling",
      "title": "Step 5 — Handle errors reliably",
      "blocks": [
        {
          "type": "p",
          "md": "Network calls fail. The table below covers the errors you'll actually encounter and what to do about each one."
        },
        {
          "type": "table",
          "headers": [
            "Error / HTTP code",
            "What it means",
            "What to do"
          ],
          "rows": [
            [
              "`401` Unauthorized",
              "API key missing, wrong, or expired",
              "Check env var; regenerate the key in the dashboard"
            ],
            [
              "`429` Too Many Requests",
              "Rate limit or quota exceeded",
              "Wait and retry with exponential backoff; check your tier's limits"
            ],
            [
              "`500 / 503`",
              "Provider-side server error",
              "Retry after a short delay; log the request ID in the error for support"
            ],
            [
              "`400` Bad Request",
              "Malformed JSON, unsupported model ID, or invalid parameter",
              "Fix the request; read the error message for the specific field"
            ],
            [
              "Timeout / network drop",
              "Connection dropped mid-call",
              "Retry with idempotent logic; log the attempt count"
            ]
          ]
        },
        {
          "type": "h3",
          "text": "Retry with exponential backoff"
        },
        {
          "type": "p",
          "md": "The `429` rate-limit error is the one you will hit most often as you scale up. The right response is to wait, then retry — but wait a bit longer each time so you don't hammer the endpoint. Here is a minimal pattern."
        },
        {
          "type": "code",
          "lang": "python",
          "title": "retry_with_backoff.py",
          "code": "import os\nimport time\nfrom openai import OpenAI, RateLimitError, APIStatusError\n\nclient = OpenAI(api_key=os.environ[\"OPENAI_API_KEY\"])\n\ndef call_with_backoff(messages, model=\"gpt-4o-mini\", max_retries=5):\n    delay = 1  # start with 1 second\n    for attempt in range(max_retries):\n        try:\n            return client.chat.completions.create(\n                model=model,\n                max_tokens=512,\n                messages=messages\n            )\n        except RateLimitError:\n            if attempt == max_retries - 1:\n                raise  # give up after max retries\n            print(f\"Rate limited. Waiting {delay}s before retry {attempt + 1}...\")\n            time.sleep(delay)\n            delay *= 2  # double the wait each time (exponential backoff)\n        except APIStatusError as e:\n            if e.status_code >= 500:  # server error — safe to retry\n                time.sleep(delay)\n                delay *= 2\n            else:\n                raise  # 4xx errors (bad request) won't fix themselves — don't retry\n\n# Usage\nresponse = call_with_backoff(\n    messages=[{\"role\": \"user\", \"content\": \"Hello!\"}]\n)\nprint(response.choices[0].message.content)"
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Both the `openai` and `anthropic` Python packages expose typed exception classes (`RateLimitError`, `AuthenticationError`, `APIConnectionError`, etc.). Catch specific exception types rather than bare `Exception` so you don't accidentally swallow bugs."
        },
        {
          "type": "h3",
          "text": "JavaScript error handling"
        },
        {
          "type": "code",
          "lang": "typescript",
          "title": "error_handling.mjs",
          "code": "import OpenAI from \"openai\";\n\nconst client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });\n\nasync function callWithRetry(messages, maxRetries = 5) {\n  let delay = 1000; // ms\n  for (let attempt = 0; attempt < maxRetries; attempt++) {\n    try {\n      return await client.chat.completions.create({\n        model: \"gpt-4o-mini\",\n        max_tokens: 512,\n        messages\n      });\n    } catch (err) {\n      if (err.status === 429 || (err.status >= 500 && err.status < 600)) {\n        if (attempt === maxRetries - 1) throw err;\n        console.log(`Retrying in ${delay}ms (attempt ${attempt + 1})...`);\n        await new Promise(r => setTimeout(r, delay));\n        delay *= 2;\n      } else {\n        throw err; // 4xx: bad request, auth — don't retry\n      }\n    }\n  }\n}\n\nconst response = await callWithRetry([\n  { role: \"user\", content: \"Hello!\" }\n]);\nconsole.log(response.choices[0].message.content);"
        }
      ]
    },
    {
      "id": "going-deeper",
      "title": "Going deeper",
      "blocks": [
        {
          "type": "p",
          "md": "Once your first call is working, the natural next steps each add one capability to the same pattern."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "split",
            "title": "Next steps from a working first call",
            "root": {
              "label": "Working API call",
              "sub": "auth + request + response + errors",
              "accent": true
            },
            "children": [
              {
                "label": "Streaming",
                "sub": "tokens arrive live, typewriter effect"
              },
              {
                "label": "Structured outputs",
                "sub": "JSON schema enforced by model"
              },
              {
                "label": "Function calling",
                "sub": "model triggers your code"
              },
              {
                "label": "Prompt caching",
                "sub": "reuse long system prompts cheaply"
              },
              {
                "label": "Embeddings",
                "sub": "turn text into vectors for search"
              }
            ]
          }
        },
        {
          "type": "p",
          "md": "**Streaming** flips one flag (`stream=True` in Python) so the response arrives token-by-token over a Server-Sent Events connection — that is how chat UIs produce the typewriter effect. The response object is replaced by an iterator you loop over. Full walkthrough: [LLM streaming explained](/learn/llm-apis/streaming-structured-outputs/llm-streaming-explained)."
        },
        {
          "type": "p",
          "md": "**Structured outputs** add a `response_format` parameter (OpenAI) or a tool schema (Anthropic) that forces the model to return JSON matching a shape you define — essential when the output feeds downstream code that needs reliable fields. **Function calling** goes further: you describe tools your code provides, and the model decides to invoke one, sending back a structured call object you execute locally and return as a result. See [what is function calling](/learn/llm-apis/function-calling/what-is-function-calling)."
        },
        {
          "type": "p",
          "md": "**Prompt caching** is a cost lever for production apps. If you have a large system prompt or a long document you include on every call, caching tells the provider to keep the computed state of that text so it doesn't reprocess it each time — you pay a fraction of the normal input-token price for cache hits. **Embeddings** are a different kind of API call entirely: instead of generating text, you get back a vector representation of your input, used for semantic search and [RAG](/learn/rag/rag-fundamentals/what-is-rag). All of these start from the same foundation you have now: a client, a key, and a request."
        },
        {
          "type": "p",
          "md": "For production deployments, add [LLM observability](/learn/production-llmops/observability-monitoring/what-is-llm-observability) early: log every request, response, token count, and latency from the start. Debugging a live app without that data is very hard. [LLM API pricing](/learn/llm-apis/cost-caching-rate-limits/llm-api-pricing) explains how to model costs before they surprise you."
        }
      ]
    }
  ],
  "faq": [
    {
      "q": "How do I authenticate with an LLM API?",
      "a": "Create an API key in the provider's dashboard (e.g. platform.openai.com or console.anthropic.com), store it as an environment variable (never hardcoded), and pass it when you create the SDK client: `OpenAI(api_key=os.environ['OPENAI_API_KEY'])`. The SDK automatically sends it as an `Authorization` header on every request."
    },
    {
      "q": "How do I read the text from an LLM API response?",
      "a": "For OpenAI, the text is at `response.choices[0].message.content`. For Anthropic (Claude), it is at `response.content[0].text`. Always check `finish_reason` (OpenAI) or `stop_reason` (Anthropic) too — if it says `'length'` or `'max_tokens'`, the reply was cut off and you may need to raise `max_tokens`."
    },
    {
      "q": "What does the temperature parameter do in an LLM API call?",
      "a": "Temperature controls output randomness. Set it near 0 for factual, deterministic answers (same input → same output every time); set it higher (0.7–1.0) for creative or varied responses. For most tasks — summarization, classification, data extraction — temperature 0 is the right default. For brainstorming or creative writing, use 0.7–1.0."
    },
    {
      "q": "Why does the LLM forget what I said in the previous message?",
      "a": "The API is stateless — each call starts fresh with no memory of earlier calls. To maintain a conversation, your code must collect all previous messages into a list and resend the entire list with every new call. The model reads the full history each time, giving the appearance of memory."
    },
    {
      "q": "What is a 429 rate limit error and how do I fix it?",
      "a": "A 429 means you've exceeded the provider's request or token rate limit for your account tier. The right fix is to catch the error, wait a few seconds, and retry — doubling the wait time each attempt (exponential backoff). Do not retry immediately in a tight loop, as that will keep triggering the limit. Over time, upgrading your usage tier raises the limits."
    },
    {
      "q": "How do I make an LLM API call in JavaScript?",
      "a": "Install the `openai` npm package, create a client with `new OpenAI({ apiKey: process.env.OPENAI_API_KEY })`, and call `await client.chat.completions.create({ model, max_tokens, messages })`. The whole pattern is `async/await` — put your call inside an `async` function or use top-level await in an ES module (`.mjs` file or `\"type\": \"module\"` in package.json)."
    }
  ],
  "related": [
    "what-is-llm-api",
    "claude-api-beginners-guide",
    "llm-streaming-explained",
    "what-is-function-calling",
    "llm-api-pricing"
  ],
  "furtherReading": [
    {
      "title": "OpenAI API quickstart — official docs",
      "url": "https://platform.openai.com/docs/quickstart"
    },
    {
      "title": "Anthropic Messages API reference",
      "url": "https://docs.anthropic.com/en/api/messages"
    },
    {
      "title": "OpenAI API Python Tutorial — Chat Completions, Streaming & Error Handling",
      "url": "https://machinelearningplus.com/gen-ai/openai-api-python-tutorial/"
    }
  ]
}
