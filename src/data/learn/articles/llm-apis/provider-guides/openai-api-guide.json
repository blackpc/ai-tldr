{
  "slug": "openai-api-guide",
  "title": "OpenAI API: A Beginner's Guide",
  "shortTitle": "OpenAI API",
  "seoTitle": "OpenAI API: A Beginner's Guide to Chat Completions",
  "metaDescription": "Learn how to use the OpenAI API from scratch: get an API key, choose a model, send your first chat completion, and understand token pricing.",
  "keywords": [
    "openai api tutorial for beginners",
    "openai api getting started",
    "chat completions api example",
    "openai api key setup python",
    "gpt api guide",
    "openai api token pricing"
  ],
  "difficulty": "beginner",
  "oneLiner": "Go from zero to your first OpenAI API call, understanding model choices, the Chat Completions format, the newer Responses API, and how token billing works.",
  "updated": "2026-06-12",
  "sections": [
    {
      "id": "in-plain-english",
      "title": "In plain English",
      "blocks": [
        {
          "type": "p",
          "md": "The **OpenAI API** is how your code talks to OpenAI's family of GPT models. When you use ChatGPT in a browser, a person is typing. The API is the same intelligence, but a *program* sends the message and a *program* reads the answer — no human required."
        },
        {
          "type": "image",
          "url": "/learn-media/dlg-067.jpg",
          "alt": "OpenAI API — diagram",
          "caption": "OpenAI API",
          "credit": "first.institute"
        },
        {
          "type": "p",
          "md": "Think of it as a delivery window for AI. You hand your request through the window (some text, a model name, a secret key), and a JSON response comes back containing the model's reply. OpenAI runs the model on its own servers — you don't download anything, you just send an HTTPS request and get an answer."
        },
        {
          "type": "p",
          "md": "OpenAI currently offers two main request styles: the **Chat Completions API** — the classic endpoint that has powered GPT apps since 2023 — and the newer **Responses API**, launched in 2025, which adds built-in tools and server-side state management. Beginners can start with Chat Completions, since it's simpler, and the concepts transfer directly if you later switch. The general pattern shared by all LLM providers is explained in [what is an LLM API](/learn/llm-apis/api-basics/what-is-llm-api)."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "**API** stands for *Application Programming Interface* — a defined contract for how one program asks another to do something. The OpenAI API is the specific set of rules for asking GPT models to generate text."
        }
      ]
    },
    {
      "id": "why-it-matters",
      "title": "Why it matters",
      "blocks": [
        {
          "type": "p",
          "md": "ChatGPT the website is for one person typing one question. The moment you want GPT *inside* something — a customer-support bot answering thousands of tickets, a script that classifies 100,000 rows of data overnight, a feature in your app that drafts replies — you need the API. A person can't sit there pasting text into a browser at that scale. Code can."
        },
        {
          "type": "p",
          "md": "The API is what turns \"GPT is a clever assistant\" into \"GPT is a building block I can ship\". It's the difference between using a calculator and wiring a calculator chip into your own product."
        },
        {
          "type": "h3",
          "text": "What you can build with it"
        },
        {
          "type": "list",
          "items": [
            "**Chatbots and assistants** — conversational UIs backed by GPT instead of canned responses.",
            "**Content pipelines** — generate, summarize, translate, or rewrite text in bulk.",
            "**Code helpers** — autocomplete, explain, or review code inside your own editor or CI tool.",
            "**Data extraction** — parse unstructured text (emails, PDFs, reviews) into structured JSON.",
            "**Agents** — programs that reason step-by-step and call external tools to complete open-ended tasks."
          ]
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "The Chat Completions request format (a list of messages with `role` and `content`) is nearly identical across providers. Learning it here transfers directly to Google Gemini, Mistral, and most open-source-model APIs."
        }
      ]
    },
    {
      "id": "how-it-works",
      "title": "How it works",
      "blocks": [
        {
          "type": "p",
          "md": "Every Chat Completions request goes to one endpoint: `POST https://api.openai.com/v1/chat/completions`. You supply a model name, a list of messages, and your API key as a header. OpenAI runs the model, then sends back a JSON object containing the reply, token counts, and the reason the model stopped."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "flow",
            "title": "One OpenAI API request, start to finish",
            "steps": [
              {
                "label": "Your code",
                "sub": "builds request: model, messages, optional settings"
              },
              {
                "label": "HTTPS + Authorization header",
                "sub": "Bearer sk-... sent to api.openai.com",
                "accent": true
              },
              {
                "label": "OpenAI runs the model",
                "sub": "GPU cluster generates a reply token-by-token"
              },
              {
                "label": "JSON response",
                "sub": "choices[0].message.content, usage, finish_reason"
              },
              {
                "label": "Your code reads it",
                "sub": "extract the text and use it in your app"
              }
            ]
          }
        },
        {
          "type": "h3",
          "text": "The messages array"
        },
        {
          "type": "p",
          "md": "The core of every request is the `messages` array. Each element has a `role` and `content`. The three roles are:"
        },
        {
          "type": "list",
          "items": [
            "**`system`** — background instructions that frame the whole conversation (\"You are a terse SQL expert.\"). Sent once, usually at the top of the array.",
            "**`user`** — the human's input.",
            "**`assistant`** — the model's previous replies. You resend these to give GPT memory of earlier turns."
          ]
        },
        {
          "type": "code",
          "lang": "json",
          "code": "{\n  \"model\": \"gpt-5.5\",\n  \"messages\": [\n    { \"role\": \"system\",    \"content\": \"You are a helpful assistant.\" },\n    { \"role\": \"user\",      \"content\": \"What is a transformer model?\" }\n  ]\n}"
        },
        {
          "type": "h3",
          "text": "The API is stateless"
        },
        {
          "type": "p",
          "md": "OpenAI keeps no session between your calls. If you want GPT to remember turn 1 when you're on turn 5, you have to resend turns 1 through 4 every time. This is the single biggest surprise for beginners: **you manage the conversation history yourself.** The newer Responses API can optionally store state on OpenAI's side (more on that below), but Chat Completions has no memory at all."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "compare",
            "title": "Chat Completions vs. Responses API — key differences",
            "columns": [
              {
                "title": "Chat Completions (classic)",
                "items": [
                  "You maintain message history",
                  "Stateless — no server-side memory",
                  "Simple, widely-supported format",
                  "Best for lightweight, stateless flows"
                ]
              },
              {
                "title": "Responses API (2025+)",
                "accent": true,
                "items": [
                  "Optional server-side conversation state",
                  "Built-in tools: web search, code interpreter, file search",
                  "Better cache hit rates (40–80% improvement)",
                  "Where future OpenAI features will land"
                ]
              }
            ]
          }
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "OpenAI recommends new projects use the **Responses API**, but Chat Completions remains fully supported. For beginners, Chat Completions is easier to understand and the concepts transfer directly. The Responses API uses the same model names and token-based billing."
        }
      ]
    },
    {
      "id": "your-first-call",
      "title": "Your first call, step by step",
      "blocks": [
        {
          "type": "p",
          "md": "Four steps from nothing to a working request."
        },
        {
          "type": "list",
          "ordered": true,
          "items": [
            "**Get an API key.** Sign up at [platform.openai.com](https://platform.openai.com), open *API keys*, and click *Create new secret key*. It starts with `sk-...`. Treat it like a password — anyone who has it can bill your account.",
            "**Store it safely.** Never paste your key into source code or commit it to git. Set it as an environment variable: `export OPENAI_API_KEY=sk-...` on macOS/Linux, or add it to a `.env` file with python-dotenv. The official SDK reads `OPENAI_API_KEY` automatically.",
            "**Install the SDK.** OpenAI publishes official libraries. For Python: `pip install openai`.",
            "**Send the request** and print the reply."
          ]
        },
        {
          "type": "code",
          "lang": "bash",
          "code": "pip install openai\nexport OPENAI_API_KEY=sk-...   # replace with your real key"
        },
        {
          "type": "code",
          "lang": "python",
          "code": "from openai import OpenAI\n\n# Reads OPENAI_API_KEY from the environment automatically.\nclient = OpenAI()\n\nresponse = client.chat.completions.create(\n    model=\"gpt-5.5\",\n    messages=[\n        {\"role\": \"system\", \"content\": \"You are a helpful assistant.\"},\n        {\"role\": \"user\",   \"content\": \"Explain what a neural network is in two sentences.\"},\n    ],\n)\n\n# The reply text lives at choices[0].message.content.\nprint(response.choices[0].message.content)\nprint(\"Tokens used:\", response.usage.prompt_tokens, \"in /\", response.usage.completion_tokens, \"out\")"
        },
        {
          "type": "p",
          "md": "Run it and you'll get a two-sentence answer plus token counts. That's a complete OpenAI integration. Everything else — streaming, tool calls, structured output — is variations on this."
        },
        {
          "type": "h3",
          "text": "Multi-turn conversation"
        },
        {
          "type": "p",
          "md": "To continue a conversation, append the model's reply to your messages list and send the whole thing again. Remember: the API is stateless — you own the history."
        },
        {
          "type": "code",
          "lang": "python",
          "code": "from openai import OpenAI\n\nclient = OpenAI()\n\n# Build up the conversation list manually.\nmessages = [\n    {\"role\": \"system\", \"content\": \"You are a concise assistant.\"},\n    {\"role\": \"user\",   \"content\": \"My favorite language is Python.\"},\n]\n\nfirst = client.chat.completions.create(model=\"gpt-5.5\", messages=messages)\nassistant_reply = first.choices[0].message.content\n\n# Append GPT's reply, then ask a follow-up.\nmessages.append({\"role\": \"assistant\", \"content\": assistant_reply})\nmessages.append({\"role\": \"user\",      \"content\": \"What's a quick tip for that language?\"})\n\nsecond = client.chat.completions.create(model=\"gpt-5.5\", messages=messages)\nprint(second.choices[0].message.content)  # GPT knows the context from turn 1."
        },
        {
          "type": "callout",
          "tone": "warn",
          "md": "Prefer raw HTTP? You can call the endpoint with `curl` or any HTTP client by setting the `Authorization: Bearer sk-...` header. But the official SDK handles auth, retries, backoff, and streaming — for beginners it's almost always the right choice."
        }
      ]
    },
    {
      "id": "models-and-pricing",
      "title": "Model lineup and token billing",
      "blocks": [
        {
          "type": "p",
          "md": "\"GPT\" isn't one model — it's a family. You choose a model per request via the `model` field. OpenAI's current lineup ranges from small, ultra-cheap models for high-volume simple tasks up to large frontier models for hard reasoning. The headline models are the GPT-5 series — the flagship (e.g. GPT-5.5) for top reasoning, plus smaller mini and nano tiers for cheaper, high-volume work."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "stack",
            "title": "OpenAI model tiers — speed vs. capability trade-off",
            "layers": [
              {
                "label": "GPT-5 Pro / frontier",
                "sub": "Best reasoning, highest cost — use for genuinely hard tasks"
              },
              {
                "label": "GPT-5 flagship",
                "sub": "Strong all-round — coding, analysis, chat — mid price"
              },
              {
                "label": "GPT-5 mini",
                "sub": "Balanced budget option — most everyday tasks"
              },
              {
                "label": "GPT-5 nano",
                "sub": "Fastest, cheapest — classification, routing, simple extraction"
              }
            ]
          }
        },
        {
          "type": "p",
          "md": "A common beginner mistake is reaching for the biggest model for everything. Resist it. A nano or mini model handles classification or simple reformatting just as well for a fraction of the price. Start small and step up only when quality falls short."
        },
        {
          "type": "h3",
          "text": "How you're billed"
        },
        {
          "type": "p",
          "md": "OpenAI charges **per token**, split into two rates: a lower price for **input tokens** (everything you send — system prompt, user messages, and conversation history) and a higher price for **output tokens** (what the model generates). Output typically costs 4–8× more than input. The exact rates depend on the model — check [platform.openai.com/pricing](https://platform.openai.com/pricing) for current numbers, as they change with new model releases."
        },
        {
          "type": "p",
          "md": "A [token](/learn/llm-fundamentals/tokens-and-tokenization/what-is-a-token) is roughly ¾ of an English word — \"chatbot\" is one token, \"unbelievable\" might be three. The `usage` field in every response tells you exactly how many input and output tokens were consumed for that call."
        },
        {
          "type": "callout",
          "tone": "warn",
          "md": "In a multi-turn conversation you resend the entire message history every call. That means turn 1's text is billed as input on every subsequent call. A 20-turn chat effectively charges you for turn 1 twenty times. Watch your `usage.prompt_tokens` as conversations grow — it's the main source of surprise bills."
        },
        {
          "type": "h3",
          "text": "Prompt caching"
        },
        {
          "type": "p",
          "md": "OpenAI automatically caches the beginning of your prompt when the same prefix appears in multiple requests. Cached input tokens are discounted (often around 50% off). This means: put your large, unchanging system prompt *first* in the messages array so it qualifies for caching. The full picture of input, output, and cached pricing is in [LLM API pricing explained](/learn/llm-apis/cost-caching-rate-limits/llm-api-pricing)."
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Check `response.usage` on every call while you're learning. Seeing real token counts is the fastest way to build cost intuition — and catch a runaway prompt before the bill does."
        }
      ]
    },
    {
      "id": "going-deeper",
      "title": "Going deeper",
      "blocks": [
        {
          "type": "p",
          "md": "Once your first call works, the same API unlocks everything serious AI apps need. These are all extra parameters on the request you already know — not separate APIs."
        },
        {
          "type": "h3",
          "text": "Streaming"
        },
        {
          "type": "p",
          "md": "By default you wait for the entire reply, then receive it at once — which can mean staring at a blank screen for several seconds on a long answer. **Streaming** sends tokens as they're generated, producing the typewriter effect you see in ChatGPT. Set `stream=True` in your request. The SDK provides a streaming helper that handles the server-sent-events protocol for you. Details in [streaming explained](/learn/llm-apis/streaming-structured-outputs/llm-streaming-explained)."
        },
        {
          "type": "h3",
          "text": "Function calling / tool use"
        },
        {
          "type": "p",
          "md": "GPT can't check live data on its own. **Function calling** lets you describe tools the model may invoke (e.g., `get_weather(city: str)`). When GPT decides a tool is needed, it returns a structured JSON request instead of prose; your code executes the function and feeds the result back. This is the foundation of agentic apps. See [what is function calling](/learn/llm-apis/function-calling/what-is-function-calling)."
        },
        {
          "type": "h3",
          "text": "Structured outputs"
        },
        {
          "type": "p",
          "md": "Pass a JSON Schema in the `response_format` field and OpenAI guarantees the reply conforms to it — no more regex-parsing freeform text when a downstream program needs structured data. For extraction tasks (\"parse this invoice into JSON\") this is a game-changer for reliability."
        },
        {
          "type": "h3",
          "text": "Error handling and rate limits"
        },
        {
          "type": "p",
          "md": "Real apps hit bumps. A `429` means you're sending too fast (rate-limited); a `500` or `503` means OpenAI's servers are struggling. The official SDK retries transient errors automatically with exponential backoff. Wrap calls in try/except for the rest:"
        },
        {
          "type": "code",
          "lang": "python",
          "code": "import openai\nfrom openai import OpenAI\n\nclient = OpenAI()\n\ntry:\n    response = client.chat.completions.create(\n        model=\"gpt-5.5\",\n        messages=[{\"role\": \"user\", \"content\": \"Hello!\"}],\n    )\n    # finish_reason is \"stop\" on a normal completion;\n    # \"length\" means it hit max_tokens and was cut off.\n    if response.choices[0].finish_reason == \"length\":\n        print(\"Reply truncated — raise max_tokens.\")\n    print(response.choices[0].message.content)\nexcept openai.RateLimitError:\n    print(\"Rate-limited. The SDK already retried — back off further.\")\nexcept openai.APIStatusError as e:\n    print(f\"API error {e.status_code}: {e.message}\")"
        },
        {
          "type": "table",
          "headers": [
            "HTTP code",
            "Means",
            "What to do"
          ],
          "rows": [
            [
              "401",
              "Invalid API key",
              "Check OPENAI_API_KEY is set and the key is active"
            ],
            [
              "404",
              "Unknown model ID",
              "Fix the model string — typos and old names both return 404"
            ],
            [
              "429",
              "Rate limited",
              "Slow down; SDK retries with backoff already"
            ],
            [
              "500/503",
              "OpenAI server error",
              "Retry later; the SDK handles this automatically"
            ]
          ]
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Production tip: rate limits are measured per organization in requests-per-minute and tokens-per-minute. They increase as your usage tier grows. Design for them from day one — queue work, retry with backoff, and avoid firing hundreds of parallel calls at the same instant."
        }
      ]
    }
  ],
  "faq": [
    {
      "q": "What is the OpenAI API in simple terms?",
      "a": "It's a way for your code to send text to OpenAI's GPT models and get an AI-generated reply back — without using ChatGPT's website. You make an HTTPS request to api.openai.com with your question, a model name, and a secret API key. OpenAI runs the model and returns the answer as JSON. It's how you embed GPT inside an app, script, or automated workflow."
    },
    {
      "q": "How do I get an OpenAI API key?",
      "a": "Sign up at platform.openai.com, open the *API keys* section under your account, and click *Create new secret key*. The key starts with `sk-`. Store it in an environment variable named `OPENAI_API_KEY` — never paste it into source code or commit it to a repository. If a key leaks, revoke it immediately in the dashboard."
    },
    {
      "q": "What is the difference between Chat Completions and the Responses API?",
      "a": "Chat Completions (the classic endpoint at /v1/chat/completions) is stateless — you manage the conversation history yourself. The Responses API, launched in 2025, can store conversation state on OpenAI's servers so you don't have to resend the full history every turn. It also has built-in tools like web search and code execution, and better prompt caching efficiency. OpenAI recommends new projects use the Responses API, but Chat Completions remains fully supported."
    },
    {
      "q": "How much does the OpenAI API cost?",
      "a": "You're billed per token with separate rates for input (what you send) and output (what the model generates). Output typically costs 4–8× more than input. Rates vary by model — cheaper mini and nano models cost a fraction of the flagship models. In multi-turn conversations, input tokens grow each call because you resend the whole history, which is why prompt caching (automatic on eligible requests, often ~50% off) matters. Check platform.openai.com/pricing for current numbers."
    },
    {
      "q": "Which OpenAI model should I use as a beginner?",
      "a": "Don't default to the biggest model. Start with a smaller, cheaper tier like GPT-5 mini or nano for most tasks — they're fast, cheap, and handle the vast majority of classification, extraction, and chat jobs well. Step up to the GPT-5 flagship (e.g. `gpt-5.5`) when you need stronger reasoning or better instruction-following. Reserve the top frontier models for genuinely hard tasks where quality differences are measurable."
    },
    {
      "q": "Does the OpenAI Chat Completions API remember previous messages?",
      "a": "No. Chat Completions is stateless — each call starts fresh. If you want GPT to recall an earlier turn, you must resend the entire message history (all prior user and assistant turns) with every new request. You maintain this list yourself in your code. The newer Responses API can optionally manage this state on OpenAI's side, which is one reason it's recommended for new projects."
    }
  ],
  "related": [
    "what-is-llm-api",
    "claude-api-beginners-guide",
    "what-is-a-token",
    "llm-api-pricing",
    "what-is-function-calling"
  ],
  "furtherReading": [
    {
      "title": "OpenAI API Text Generation (official docs)",
      "url": "https://platform.openai.com/docs/guides/text"
    },
    {
      "title": "OpenAI Chat Completions API Reference",
      "url": "https://developers.openai.com/api/reference/chat-completions/overview"
    },
    {
      "title": "OpenAI API Pricing",
      "url": "https://openai.com/api/pricing/"
    }
  ]
}
