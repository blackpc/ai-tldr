{
  "slug": "what-is-axolotl",
  "title": "What Is Axolotl? Config-Driven LLM Fine-Tuning",
  "shortTitle": "Axolotl",
  "seoTitle": "What Is Axolotl? YAML-Config LLM Fine-Tuning Tool",
  "metaDescription": "Axolotl is an open-source fine-tuning wrapper that runs SFT, LoRA, QLoRA and RLHF from a single YAML config for reproducible multi-GPU training.",
  "keywords": [
    "axolotl",
    "what is axolotl",
    "axolotl fine-tuning",
    "axolotl yaml config",
    "axolotl vs unsloth",
    "axolotl lora qlora",
    "multi-gpu fine-tuning"
  ],
  "difficulty": "intermediate",
  "oneLiner": "You will understand how Axolotl turns a single YAML file into a reproducible fine-tuning run and when its config-driven, multi-GPU approach is the right fit.",
  "updated": "2026-06-14",
  "sections": [
    {
      "id": "in-plain-english",
      "title": "In plain English",
      "blocks": [
        {
          "type": "p",
          "md": "[Fine-tuning](/learn/fine-tuning/fine-tuning-basics/what-is-fine-tuning) an open LLM by hand means stitching together a lot of moving parts: load the base model, tokenize your dataset, apply the right chat template, wire up [LoRA or QLoRA](/learn/fine-tuning/fine-tuning-basics/full-fine-tuning-vs-peft), set the optimizer and learning-rate schedule, shard everything across GPUs, log metrics, and save checkpoints. Each piece is a chance to make a quiet mistake that ruins the run."
        },
        {
          "type": "image",
          "url": "/learn-media/dlg-620.jpg",
          "alt": "Axolotl — illustration",
          "caption": "Axolotl",
          "credit": "labelyourdata.com"
        },
        {
          "type": "p",
          "md": "**Axolotl** is an open-source tool that turns all of that into a single configuration file. You write a **YAML** file that says *which model*, *which dataset*, and *which method* (plain supervised fine-tuning, LoRA, QLoRA, or preference training), and Axolotl runs the whole training job for you — including across multiple GPUs. It is a wrapper around the standard Hugging Face training stack, so it is not reinventing training; it is packaging the proven pieces behind one clean config."
        },
        {
          "type": "p",
          "md": "Think of it like a recipe card versus cooking from instinct. Cooking by hand, you might nail it once but never reproduce it exactly. A written recipe — *exact ingredients, exact steps* — means anyone on your team can run the same dish and get the same result. Axolotl's YAML is that recipe card for a fine-tuning run: hand it to a teammate or a CI server, and they get the same model you did."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "The name is a play on the axolotl, a salamander famous for *regenerating* lost body parts — a nod to repeatable, reproducible training runs. The project is community-driven and actively maintained."
        }
      ]
    },
    {
      "id": "why-it-matters",
      "title": "Why it matters",
      "blocks": [
        {
          "type": "p",
          "md": "The hard part of fine-tuning is rarely the *idea*. It is the dozens of small, error-prone details between your dataset and a trained model. Axolotl exists to absorb that plumbing so you spend your effort on the things that actually decide quality: the data and the method."
        },
        {
          "type": "list",
          "items": [
            "**Reproducibility.** A run is fully described by one text file you can commit to git. Six months later you can re-run the *exact* job, diff two configs to see what changed, and trust that 'it worked on my machine' means it works everywhere.",
            "**Fewer silent mistakes.** Getting the [chat template and loss masking](/learn/fine-tuning/fine-tuning-basics/chat-templates-and-loss-masking) right by hand is a classic footgun — train on the wrong tokens and the model learns the wrong thing while loss still looks fine. Axolotl handles common dataset formats and templates for you.",
            "**Multi-GPU without the pain.** Sharding a model across several GPUs (via DeepSpeed or FSDP) is fiddly to set up correctly. Axolotl exposes it as a few config lines instead of a deep distributed-training project.",
            "**Method coverage in one place.** Full fine-tuning, LoRA, QLoRA, and preference/RLHF-style training all live behind the same config schema, so switching approaches is a config edit, not a rewrite."
          ]
        },
        {
          "type": "p",
          "md": "Who cares most? Teams and individuals fine-tuning **open-weight** models (Llama, Qwen, Mistral, and friends) who want runs they can repeat, review, and hand off. If you are tuning a closed model through a provider API, that provider already hides this plumbing — Axolotl is for the self-hosted, open-weight world where *you* own the training loop. It is a popular workhorse precisely because it makes that world feel almost as turnkey as a hosted API, without giving up control."
        }
      ]
    },
    {
      "id": "how-it-works",
      "title": "How it works",
      "blocks": [
        {
          "type": "p",
          "md": "Axolotl sits *on top of* the Hugging Face ecosystem. Your YAML config is the input; from it, Axolotl assembles and runs a real training job using libraries you would otherwise call yourself — Transformers for the model, PEFT for LoRA/QLoRA adapters, bitsandbytes for quantization, TRL for preference training, and DeepSpeed or FSDP for multi-GPU sharding."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "flow",
            "title": "From one YAML file to a trained model",
            "steps": [
              {
                "label": "YAML config",
                "sub": "model + data + method"
              },
              {
                "label": "Load base model",
                "sub": "Transformers, quantization"
              },
              {
                "label": "Prepare dataset",
                "sub": "tokenize, chat template, mask"
              },
              {
                "label": "Train",
                "sub": "LoRA/QLoRA/SFT, multi-GPU"
              },
              {
                "label": "Output",
                "sub": "adapter or merged model",
                "accent": true
              }
            ]
          }
        },
        {
          "type": "h3",
          "text": "The config is the whole interface"
        },
        {
          "type": "p",
          "md": "Almost everything you would normally write in Python becomes a field in the YAML. A trimmed-down config gives the shape of it: pick a base model, point at a dataset, choose the method, and set the core [hyperparameters](/learn/fine-tuning/fine-tuning-basics/fine-tuning-hyperparameters)."
        },
        {
          "type": "code",
          "lang": "yaml",
          "title": "qlora-example.yaml (illustrative)",
          "code": "base_model: meta-llama/Llama-3.1-8B\n\n# Method: 4-bit QLoRA (load_in_4bit + adapter: lora)\nload_in_4bit: true\nadapter: lora\nlora_r: 16\nlora_alpha: 32\nlora_dropout: 0.05\n\n# Dataset: a known format Axolotl knows how to template\ndatasets:\n  - path: tatsu-lab/alpaca\n    type: alpaca\n\n# Core training hyperparameters\nsequence_len: 2048\nmicro_batch_size: 2\ngradient_accumulation_steps: 4\nnum_epochs: 3\nlearning_rate: 0.0002\noptimizer: adamw_bnb_8bit\n\noutput_dir: ./out"
        },
        {
          "type": "p",
          "md": "The `type: alpaca` line is doing quiet, important work: it tells Axolotl how each dataset row maps onto a prompt and a response so the model trains on the *answer* tokens, not the instruction tokens. Axolotl ships handling for several common dataset shapes, so you usually do not write tokenization or templating code at all."
        },
        {
          "type": "h3",
          "text": "Running it"
        },
        {
          "type": "p",
          "md": "Once the config exists, the run is one command. The same command scales to multiple GPUs through an accelerate launch, because the distributed setup is also described in config rather than in your code."
        },
        {
          "type": "code",
          "lang": "bash",
          "title": "launch a run",
          "code": "# Single command, single config file\naxolotl train qlora-example.yaml\n\n# Same job across multiple GPUs (config controls the sharding)\naccelerate launch -m axolotl.cli.train qlora-example.yaml"
        },
        {
          "type": "callout",
          "tone": "tip",
          "md": "Because the config fully describes the run, the cleanest workflow is: copy an example config, change a few fields, commit it next to your dataset, and let CI run it. The config *is* the documentation of what you trained."
        }
      ]
    },
    {
      "id": "what-the-config-covers",
      "title": "What the config actually covers",
      "blocks": [
        {
          "type": "p",
          "md": "It helps to see the config as four groups of decisions. Almost every field you will touch falls into one of these buckets, which maps neatly onto the choices any fine-tuning job has to make."
        },
        {
          "type": "table",
          "headers": [
            "Config area",
            "What it decides",
            "Example fields"
          ],
          "rows": [
            [
              "Model",
              "Which base model and how it is loaded",
              "`base_model`, `load_in_4bit`, `load_in_8bit`"
            ],
            [
              "Method",
              "Full fine-tune vs adapter, and adapter shape",
              "`adapter`, `lora_r`, `lora_alpha`, `lora_target_modules`"
            ],
            [
              "Data",
              "Which dataset and how rows become prompts",
              "`datasets`, `type`, `sequence_len`, `chat_template`"
            ],
            [
              "Training",
              "The optimization knobs and hardware use",
              "`learning_rate`, `num_epochs`, `micro_batch_size`, `deepspeed`"
            ]
          ]
        },
        {
          "type": "p",
          "md": "This is why switching methods is cheap. Going from a memory-hungry full fine-tune to a lightweight **QLoRA** run is mostly flipping `load_in_4bit` on and setting `adapter: lora` — the model, data, and most training fields stay put. The same config skeleton stretches from a tiny experiment on one GPU to a serious multi-GPU job."
        }
      ]
    },
    {
      "id": "axolotl-vs-alternatives",
      "title": "Axolotl vs hand-written code vs other tools",
      "blocks": [
        {
          "type": "p",
          "md": "Axolotl is not the only way to fine-tune an open model. The honest trade-off is *control vs convenience vs reproducibility*, and different tools sit at different points on that line."
        },
        {
          "type": "diagram",
          "diagram": {
            "kind": "compare",
            "title": "Three ways to fine-tune an open model",
            "columns": [
              {
                "title": "Axolotl",
                "accent": true,
                "items": [
                  "One YAML describes the whole run",
                  "Built-in multi-GPU (DeepSpeed/FSDP)",
                  "Great for reproducible team workflows",
                  "Less flexible than raw code for odd cases"
                ]
              },
              {
                "title": "Hand-written code",
                "items": [
                  "You call Transformers/PEFT/TRL yourself",
                  "Total flexibility for custom logic",
                  "Every detail is your responsibility",
                  "Easy to make silent, hard-to-find mistakes"
                ]
              },
              {
                "title": "Other wrappers",
                "items": [
                  "Some optimize single-GPU speed/memory",
                  "Some add a no-code web UI",
                  "Different defaults and dataset formats",
                  "Pick the one matching your workflow"
                ]
              }
            ]
          }
        },
        {
          "type": "p",
          "md": "A common comparison is **Axolotl vs Unsloth**. They solve overlapping problems differently: Unsloth focuses on squeezing maximum speed and minimum memory out of a single GPU, while Axolotl focuses on config-driven, reproducible, and *multi-GPU* runs. They are not strictly rivals — some teams prototype quickly with one and run scaled, repeatable jobs with the other. The right pick depends on whether your bottleneck is one-GPU efficiency or team-wide reproducibility across more hardware."
        },
        {
          "type": "callout",
          "tone": "note",
          "md": "Axolotl does not change *what* fine-tuning is or *whether* you should do it — for that, see [when to fine-tune](/learn/fine-tuning/fine-tuning-basics/when-to-fine-tune). It only changes how cleanly you run it once you have decided to."
        }
      ]
    },
    {
      "id": "common-pitfalls",
      "title": "Common pitfalls",
      "blocks": [
        {
          "type": "p",
          "md": "Axolotl removes a lot of plumbing mistakes, but a config-driven tool has its own failure modes. Most trouble comes from the config being *valid* yet not describing what you actually meant."
        },
        {
          "type": "list",
          "items": [
            "**Wrong dataset `type`.** If your data does not match the format you declared, rows get templated incorrectly and the model trains on the wrong tokens. Always sanity-check a few tokenized samples before committing to a long run.",
            "**Mismatched chat template.** Fine-tune with one template, then serve with a different one, and quality collapses at inference time. Keep the [chat template](/learn/fine-tuning/fine-tuning-basics/chat-templates-and-loss-masking) consistent between training and deployment.",
            "**Out-of-memory surprises.** `sequence_len`, `micro_batch_size`, and whether you use 4-bit loading dominate memory. If a run OOMs, lower these or enable QLoRA before reaching for more GPUs.",
            "**Copy-pasting a whole example without reading it.** Example configs carry opinionated defaults (learning rate, epochs, target modules). Skim every field — a default that suits a 70B model may overfit a small one. Watch for [overfitting signs](/learn/fine-tuning/fine-tuning-basics/fine-tuning-overfitting-signs).",
            "**Forgetting it is still real training.** A clean config does not guarantee a good model. You still need good data, sane hyperparameters, and an honest [evaluation](/learn/fine-tuning/fine-tuning-basics/evaluate-fine-tuned-model) afterward."
          ]
        },
        {
          "type": "callout",
          "tone": "warn",
          "md": "Because Axolotl wraps fast-moving upstream libraries, pin your environment. Reproducing a config a year later also means reproducing the library versions it ran against — record them alongside the YAML."
        }
      ]
    },
    {
      "id": "going-deeper",
      "title": "Going deeper",
      "blocks": [
        {
          "type": "p",
          "md": "Once the basic config flow clicks, the useful next steps are mostly about scale, data, and method depth."
        },
        {
          "type": "p",
          "md": "**Multi-GPU and sharding.** For models too big to fit on one card, you point the config at a DeepSpeed (ZeRO) or FSDP setup. These shard the model weights, gradients, and optimizer state across GPUs so the job fits in aggregate memory. Axolotl exposes this as configuration, but understanding *why* you are using ZeRO stage 2 vs 3, or FSDP, is what lets you debug an OOM on a large run."
        },
        {
          "type": "p",
          "md": "**Sample packing and sequence length.** To use GPU time efficiently, Axolotl can pack multiple short examples into one sequence up to `sequence_len`. This speeds training but interacts subtly with loss masking and attention boundaries, so it is worth understanding before you trust the throughput gains on a quality-sensitive run."
        },
        {
          "type": "p",
          "md": "**Beyond plain SFT.** The same config schema reaches into preference training (DPO and related RLHF-style methods) via the underlying TRL library. That means the jump from instruction tuning to preference optimization is, again, mostly a config change — though the *data* you need (preference pairs, not just instruction-response rows) is fundamentally different, which is the real work."
        },
        {
          "type": "p",
          "md": "**Where to go next.** Solidify the fundamentals the tool sits on: [full fine-tuning vs PEFT](/learn/fine-tuning/fine-tuning-basics/full-fine-tuning-vs-peft) to understand the methods, [dataset preparation](/learn/fine-tuning/fine-tuning-basics/fine-tuning-dataset-prep) because data quality outranks every config knob, and the [cost breakdown](/learn/fine-tuning/fine-tuning-basics/fine-tuning-cost-breakdown) so a multi-GPU run does not surprise you on the bill. The durable lesson: Axolotl makes runs reproducible and repeatable, but it cannot make a bad dataset good — the config is only as valuable as the data and evaluation behind it."
        }
      ]
    }
  ],
  "faq": [
    {
      "q": "What is Axolotl used for?",
      "a": "Axolotl is an open-source tool for fine-tuning open-weight LLMs from a single YAML config. You describe the model, dataset, and method (full fine-tuning, LoRA, QLoRA, or preference training) and it runs the whole job, including across multiple GPUs, so runs are reproducible and easy to hand off."
    },
    {
      "q": "Does Axolotl support LoRA and QLoRA?",
      "a": "Yes. You select the method in the config: set `adapter: lora` for LoRA, and add `load_in_4bit: true` to turn it into QLoRA. Full fine-tuning and preference/RLHF-style training are also available through the same config schema, so switching method is mostly a config edit."
    },
    {
      "q": "Axolotl vs Unsloth — which should I use?",
      "a": "They overlap but optimize for different things. Unsloth focuses on maximum speed and minimum memory on a single GPU, while Axolotl focuses on config-driven, reproducible, multi-GPU runs. Choose based on whether your bottleneck is one-GPU efficiency or team-wide reproducibility across more hardware."
    },
    {
      "q": "Do I need to write Python to use Axolotl?",
      "a": "Mostly no. The training job is described in a YAML file and launched with a single command, so you usually avoid writing tokenization, templating, or training-loop code. You still benefit from understanding the underlying concepts, but day-to-day work is editing config, not code."
    },
    {
      "q": "Can Axolotl train across multiple GPUs?",
      "a": "Yes. Multi-GPU sharding via DeepSpeed (ZeRO) or FSDP is configured in the YAML and launched through an accelerate command. That is one of its main draws over single-GPU-focused tools: scaling to more hardware is a config change rather than a distributed-training project."
    }
  ],
  "related": [
    "what-is-fine-tuning",
    "full-fine-tuning-vs-peft",
    "fine-tuning-hyperparameters",
    "fine-tuning-dataset-prep",
    "chat-templates-and-loss-masking",
    "when-to-fine-tune"
  ],
  "furtherReading": [
    {
      "title": "Axolotl — GitHub repository",
      "url": "https://github.com/axolotl-ai-cloud/axolotl"
    },
    {
      "title": "Hugging Face PEFT documentation",
      "url": "https://huggingface.co/docs/peft"
    },
    {
      "title": "TRL (Transformer Reinforcement Learning) docs",
      "url": "https://huggingface.co/docs/trl"
    }
  ],
  "links": [
    "https://docs.axolotl.ai",
    "https://github.com/axolotl-ai-cloud/axolotl"
  ]
}
